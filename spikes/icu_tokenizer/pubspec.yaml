name: icu_tokenizer
description: >-
  Spike: UAX #29 word segmentation via ICU C FFI, with a pure-Dart
  RegExp fallback. Investigates feasibility for kmdb lexical search.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
