name: bge_embeddings
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  archive: ^4.0.9
  ffi: ^2.2.0
  http: ^1.6.0
  path: ^1.9.1
  icu_tokenizer:
    path: ../icu_tokenizer

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
