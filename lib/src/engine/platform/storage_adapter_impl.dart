// Default stub — used on platforms where neither dart:io nor dart:js_interop
// is available. In practice this should never be reached; if it is, the
// developer needs to use the memory adapter explicitly.
export 'storage_adapter_web.dart';
