# Changelog

## 0.1.0

First stable release, published to pub.dev. Targets `kmdb` `^0.1.0`.

Provides `HtmlTextExtractor`, a `VaultTextExtractor` for `text/html` vault
blobs, wrapping the `html` package. Pure Dart; optional, opt-in.

### Requirements

- Dart SDK `^3.13.0`.
