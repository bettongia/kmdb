name: kmdb_extractor_html
description: HTML text extractor for KMDB vault search — implements
  VaultTextExtractor for text/html blobs by walking the html package's DOM.
version: 0.1.0-dev.1
repository: https://github.com/bettongia/kmdb

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  kmdb: ^0.1.0-dev.1
  html: ^0.15.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
