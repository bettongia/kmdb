name: kmdb_google_drive
description: Google Drive sync adapter for KMDB — connects the SyncStorageAdapter
  interface to the Google Drive REST API (Drive v3).
version: 0.1.0-dev.1
repository: https://github.com/bettongia/kmdb

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  kmdb: ^0.1.0-dev.1
  googleapis: ^14.0.0
  googleapis_auth: ^2.0.0
  http: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  kmdb_harness:
