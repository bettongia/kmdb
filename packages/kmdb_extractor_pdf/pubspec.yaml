name: kmdb_extractor_pdf
description: PDF text extractor for KMDB vault search — implements
  VaultTextExtractor for application/pdf blobs by wrapping betto_pdfium
  (PDFium FFI).
version: 0.1.0-dev.1
repository: https://github.com/bettongia/kmdb

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  kmdb: ^0.1.0-dev.1
  betto_pdfium: ^0.1.0-dev.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
