name: kmdb_inferencing
description: ONNX runtime and BGE embedding model for KMDB semantic search.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  archive:
  crypto:
  ffi:
  http:
  kmdb:
  kmdb_lexical:
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.6
