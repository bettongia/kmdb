# Changelog

## 0.1.0

First stable release, published to pub.dev. Targets `kmdb` `^0.1.0`.

Provides `MarkdownTextExtractor`, a `VaultTextExtractor` for `text/markdown`
vault blobs, wrapping the `markdown` package. Pure Dart; optional, opt-in.

### Requirements

- Dart SDK `^3.13.0`.
