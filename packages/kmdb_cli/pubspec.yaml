name: kmdb_cli
description: CLI for kmdb database.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  kmdb:
    path: ../kmdb
  args: ^2.4.2
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.6
  lints: ^6.0.0
  coverage: ^1.15.0
