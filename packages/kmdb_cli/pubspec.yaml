name: kmdb_cli
description: Command-line interface for the KMDB database engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  kmdb:
    path: ../kmdb
  args: ^2.6.0

dev_dependencies:
  lints: ^6.0.0
  path: ^1.9.0
  test: ^1.25.6
