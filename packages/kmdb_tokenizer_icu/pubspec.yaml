name: kmdb_tokenizer_icu
description: ICU-backed word tokeniser for KMDB lexical search.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  ffi: ^2.1.4
  kmdb:
  kmdb_lexical:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.6
