name: kmdb_lexical
description:
  Lexical text utilities (tokenizer, stemmer, stopwords) used by the KMDB BM25
  full-text search engine.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  intl: ^0.20.2

dev_dependencies:
  code_builder: any
  betto_builder_tools:
  lints: ^6.0.0
  test: ^1.25.6
  #vendor: ^0.9.8
