# Issues

This is a tracking list for issues/concerns as they arise. Some may be non
trivial and be planned out in `plans/`. As work is completed, check it off the
task list. If you think the effort is more involved, create a plan file in
`plans/` and ask the user if they want you to start an investigation.

[ ] Some CLI calls just present the raw stack error to the user - we should
present a user-friendly message instead.

[ ] It would be useful to create a man page for the CLI
