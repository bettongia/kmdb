# Release Checklist — 0.1.0

> **Status: scaffolded, partially prepped.** This file was created by WI-9
> (`docs/plans/completed/plan_0_10_01_wi9_release_gate.md`), which promoted
> the entire `betto_*` dependency closure and every KMDB member package to a
> suffix-free `0.1.0` and removed the now-redundant `betto_*` entries from
> the root `pubspec.yaml`'s `dependency_overrides`. That work covers part of
> Stage 1 below (version numbers, dependency constraints). It does **not**
> cover CHANGELOG entries for this version, the WI-9 Phase C "W6" final
> readiness sweep (public-API audit, dead-code sweep, doc-comment audit,
> CHANGELOG accuracy), the applicable §28 RC-items, or any of Stage 2 (the
> real `dart pub publish` calls) — those are separate, later steps in the
> WI-9 plan (Phase C/D) and are **not executed by this file's creation**.

## Release metadata

- **Version:** 0.1.0
- **Date:** _(fill in when the release is actually cut)_
- **Released by:** _(fill in when the release is actually cut)_

## Stage 1 — Prep

For each Dart-publishable workspace member, confirm: `publish_to: none`
removed, `LICENSE` present, `repository:` set, `CHANGELOG.md` has an entry
for this version, every dependency has a real (non-blank) version
constraint, and the package version matches this release.

- [x] `kmdb` — `publish_to: none` absent, `LICENSE` present, `repository:`
      set, version `0.1.0`, `betto_*` `dependencies:` promoted to `^0.1.0`
      (WI-9 Phase B). `CHANGELOG.md` still only has a `0.1.0-dev.1` entry —
      **pending**, deferred to the WI-9 Phase C (W6) CHANGELOG-accuracy
      sweep, which must add a `0.1.0` entry before this release is cut.
- [x] `kmdb_cli` — same, version `0.1.0`, `kmdb`/`kmdb_google_drive`/the
      three extractors/`betto_inferencing` promoted to `^0.1.0`. CHANGELOG
      entry **pending** (same as above).
- [x] `kmdb_google_drive` — same, version `0.1.0`, `kmdb` promoted to
      `^0.1.0`. CHANGELOG entry **pending**.
- [x] `kmdb_extractor_pdf` — same, version `0.1.0`, `kmdb`/`betto_pdfium`
      promoted to `^0.1.0`. CHANGELOG entry **pending**.
- [x] `kmdb_extractor_html` — same, version `0.1.0`, `kmdb` promoted to
      `^0.1.0`. CHANGELOG entry **pending**.
- [x] `kmdb_extractor_markdown` — same, version `0.1.0`, `kmdb` promoted to
      `^0.1.0`. CHANGELOG entry **pending**.

Root `dependency_overrides`: every `betto_*` entry removed (WI-9 Phase B) —
the workspace now resolves the `betto_*` closure purely from the member
`dependencies:` constraints above, the same path a pub.dev consumer takes. A
clean `dart pub get` at the workspace root lands every `betto_*` at exactly
`0.1.0` (verified as part of WI-9 Phase B; see the plan's Summary).

## Stage 2 — Publish

Not yet executed — this is a separate, explicit, user-authorised action, and
requires the WI-9 Phase C (W6) sweep and applicable §28 RC-items to be green
first (WI-9's re-sequencing note: W6 runs against the promoted, override-free
tree as the final readiness gate before the tag).

The `publish-dryrun` CI job (`.github/workflows/cicd.yml`, backed by the
`cicd_publish_dryrun` `make_cicd.mk` target) already runs `dart pub publish
--dry-run` for the six auto-published packages below on every push/PR, so
Stage 2's real `dart pub publish` calls should never be the first time a
validation error is seen.

- [ ] `kmdb`
- [ ] `kmdb_google_drive`
- [ ] `kmdb_extractor_pdf`
- [ ] `kmdb_extractor_html`
- [ ] `kmdb_extractor_markdown`
- [ ] `kmdb_cli`
- [ ] `kmdb_flutter` (hand-published — verify the three open unknowns in
      README.md's _Hand-publishing the Flutter packages_ section first)
- [ ] `kmdb_icloud` (hand-published — same caveats as `kmdb_flutter`)

`kmdb_harness` is never published.

## §28 Release Checklist entries

Not yet executed. Go through [§28](../spec/28_release_checklist.md) **as it
stands at the time Stage 2 actually starts** (do not reuse the
`0.1.0-dev.1.md` entry list verbatim — new entries may have accrued) and fill
in one row per `RC-N` entry, as described in README.md's
_Per-release checklist convention_.

| Entry | Applies? | Result | Notes |
| :---- | :------- | :----- | :---- |
| RC-N — *(short title from §28)* | | | |

## Sign-off

- [ ] Every applicable §28 entry passes or has an accepted, documented waiver.
- [x] This file committed alongside the version bump.
