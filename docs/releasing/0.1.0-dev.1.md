# Release Checklist — 0.1.0-dev.1

> **Status: scaffolded, not executed.** This file was authored as the first
> worked example of the [TEMPLATE.md](TEMPLATE.md) process, populated against
> the current state of [§28](../spec/28_release_checklist.md) (RC-1…RC-24) as
> of 2026-07-17. Per `plan_0_09_release_process_doc.md`'s explicit scope
> boundary, this plan prepares the workspace (Stage 1) and documents the
> process — it does **not** execute the real `dart pub publish` calls or run
> the manual/out-of-band §28 checks, which require real cloud credentials,
> real hardware, and a human decision to actually cut the release. The Stage 1
> rows below reflect work genuinely completed and verified (via
> `dart pub publish --dry-run`) during this plan's implementation; the Stage 2
> and §28 rows are left unchecked, pending the actual release.

## Release metadata

- **Version:** 0.1.0-dev.1
- **Date:** _(fill in when the release is actually cut)_
- **Released by:** _(fill in when the release is actually cut)_

## Stage 1 — Prep

- [x] `kmdb` — `publish_to: none` removed, `LICENSE` present, `repository:`
      set, `CHANGELOG.md` added, all dependency constraints real (including
      `meta`, which needed fixing from `any` to `^1.18.3` — discovered via
      dry-run, not anticipated by the plan), version `0.1.0-dev.1`.
      `dart pub publish --dry-run` result: 1 warning (uncommitted
      `pubspec.yaml` — expected pre-commit), 13 hints (workspace
      `dependency_overrides` — expected, see README.md).
- [x] `kmdb_cli` — same checks, version `0.1.0-dev.1`. Dry-run: 1 warning,
      14 hints (same expected categories).
- [x] `kmdb_google_drive` — same checks, `LICENSE` added (was missing),
      version `0.1.0-dev.1`. Dry-run: 1 warning, 13 hints.
- [x] `kmdb_extractor_pdf` — same checks, `LICENSE` added (was missing),
      version `0.1.0-dev.1`. Dry-run: 1 warning, 14 hints.
- [x] `kmdb_extractor_html` — same checks, `LICENSE` added (was missing),
      version `0.1.0-dev.1`. Dry-run: 1 warning, 13 hints.
- [x] `kmdb_extractor_markdown` — same checks, `LICENSE` added (was missing),
      version `0.1.0-dev.1`. Dry-run: 1 warning, 13 hints.

All six `dart pub publish --dry-run` runs are clean modulo the two expected,
documented categories (see README.md's Stage 1 section) — no unexpected
warnings or hard errors remain.

## Stage 2 — Publish

Not yet executed — this is a separate, explicit, user-authorised action.

- [ ] `kmdb`
- [ ] `kmdb_google_drive`
- [ ] `kmdb_extractor_pdf`
- [ ] `kmdb_extractor_html`
- [ ] `kmdb_extractor_markdown`
- [ ] `kmdb_cli`
- [ ] `kmdb_flutter` (hand-published — verify the three open unknowns in
      README.md's _Hand-publishing the Flutter packages_ section first)
- [ ] `kmdb_icloud` (hand-published — same caveats as `kmdb_flutter`)

## §28 Release Checklist entries

This is the first release, so every subsystem currently shipped is being
validated against a real release for the first time — most entries apply.
None have been executed as part of this plan; `Applies?` reflects a desk
review against each entry's own **Applies when** condition, not an actual
test run.

| Entry | Applies? | Result | Notes |
| :---- | :------- | :----- | :---- |
| RC-1 — Google Drive API behaviour probe | Yes | Pending | `kmdb_google_drive` ships in this release. |
| RC-2 — Google Drive real-service sync soak | Yes | Pending | Same. |
| RC-3 — Cross-process database lock exclusivity | Yes | Pending | First release of `kmdb` to any external consumer. |
| RC-4 — Linux directory-fsync durability | Yes | Pending | Same. |
| RC-5 — Harness preset 5 (real-isolate) stress soak | Yes | Pending | Same. |
| RC-6 — Multi-device tombstone non-resurrection | Yes | Pending | Same. |
| RC-7 — Returning stale device does not resurrect deleted data | Yes | Pending | Same. |
| RC-8 — Cross-device `$ver:` purge / ingest-floor interaction | Yes | Pending | Document versioning ships. |
| RC-9 — Real-service cloud-soak via the harness framework | Yes | Pending | `kmdb_google_drive` ships. |
| RC-10 — Web (OPFS SAHPool) crash/durability verification | Yes | Pending | `kmdb` targets web. |
| RC-11 — Web (OPFS SAHPool) cross-tab lock exclusion | Yes | Pending | Same. |
| RC-12 — iCloud (CloudKit) empirical behaviour probe | Deferred | — | `kmdb_icloud` hand-published separately (Q5) — run when that package is actually released, not blocking Stage 2. |
| RC-13 — iCloud (CloudKit) real-service sync soak | Deferred | — | Same. |
| RC-14 — Model download SHA-256 verification and atomic rename | Yes | Pending | `betto_inferencing`/`ModelDownloader` reachable via `kmdb_cli`. |
| RC-15 — `betto_onnxrt` ORT binary download, session load, identity-model inference | Yes | Pending | Same. |
| RC-16 — Encryption: Argon2id timing and re-derive-per-session on web | Yes | Pending | `kmdb` ships encryption and targets web. |
| RC-17 — iOS SPM manifest: compile and link verification | Deferred | — | `kmdb_icloud` hand-published separately (Q5). |
| RC-18 — `kmdb_flutter`: DEK round-trip and native crypto acceleration | Deferred | — | `kmdb_flutter` hand-published separately (Q5). |
| RC-19 — Two-file flush crash-recovery fault injection | Yes | Pending | First release of the durability-hardened flush path. |
| RC-20 — Multi-device sync: `$$` namespace isolation | Yes | Pending | Local-only namespaces ship in `kmdb`. |
| RC-21 — Vault search isolate crash recovery | Yes | Pending | Vault search ships in `kmdb`/`kmdb_cli`. |
| RC-22 — Encryption confidentiality reconciliation: legacy database format break | Yes | Pending | First public release — no pre-reconciliation databases exist in the wild yet, but the check is cheap and confirms the documented behaviour. |
| RC-23 — `dart build cli` native-asset bundling on Linux and Windows | Yes | Pending | `kmdb_cli` ships a compiled binary. |
| RC-24 — `kmdb_cli` credential store: real-OS permission verification | Yes | Pending | `kmdb_cli` ships the credential store. |

## Sign-off

- [ ] Every applicable §28 entry passes or has an accepted, documented waiver.
- [x] This file committed alongside the version bump.
