---
title: Integration Guide Friction Log — 2026-09-11 run 2
subtitle: Cold-read re-verification of docs/integration_guide/README.md
toc-title: "Contents"
...

# Run header

| Field | Value |
| :---- | :---- |
| Guide version / commit | `e2ed4f61f1a8d40d8148c53626ca0d04adfa71aa` — "Integration guide: close B2 cold-read friction (make the guide stand alone)" |
| Date | 2026-09-11 |
| Tester identity (human, or agent type/model) | cold-read agent (general-purpose), re-verification run 2 |
| Overall outcome (did a working app build end-to-end?) | **Yes.** The full data layer (models, codecs, schemas, `AppDatabase.open()`) built verbatim from the guide, `dart analyze` reports "No issues found", and 8 headless tests pass covering open/close, schema admission, CRUD + queries, the secondary index (`explainedGet`), vault ingest/get + KVLT packaging, field + attachment-content search, two-instance sync convergence, reactivity, and the encryption bootstrap + wrong-passphrase + recovery-code fault paths. No blockers. |
| Environment | macOS (Darwin 25.6.0, arm64); Flutter 3.47.1 (stable); Dart 3.13.1; `kmdb` + `kmdb_extractor_html` + `kmdb_extractor_markdown` consumed via **path** `dependency_overrides` (pre-publication). Built as a pure-Dart package (`coldread_build/`); GUI not launched (out of scope). |

# Summary of the re-verification

The four blockers the *prior* cold read reported are all fixed: the
`AppDatabase.open()` wrapper, the `Project`/`TaskComment` models + codecs,
`Task.copyWith`, and the full three-schema set are now printed **in full** and
compile exactly as written. The guide now genuinely stands alone for building
the data layer end to end. The remaining friction is all `minor` — no blocker,
no major. Counts below.

- **blocker:** 0
- **major:** 0
- **minor:** 5

# Friction entries

| Guide section / step reference | What the guide said to do | What actually happened | Severity | Self-resolvable? (yes/no, how) | Time lost | Suspected root cause | Suggested guide fix | Environment |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| "Install kmdb" vs. "Putting it together: `AppDatabase.open()`" | "If you want vault attachment-content search … **also** add: `kmdb_extractor_html`, `kmdb_extractor_markdown`" — framed as optional. | The canonical `AppDatabase.open()` the entire rest of the guide is built on `import`s **and unconditionally instantiates** both extractors (`extractors: [HtmlTextExtractor(), MarkdownTextExtractor()]`). A reader who takes the extractors as optional and skips them cannot compile the guide's own central wrapper. | minor | yes — obvious once you copy `AppDatabase.open()` and hit the unresolved imports; add both deps. | ~2 min | The "optional" framing in Install is inconsistent with the wrapper making them mandatory. | Either (a) note in "Install kmdb" that `AppDatabase.open()` as printed requires both extractors, so they're required to follow this guide verbatim; or (b) guard the `vaultSearch:` wiring behind a comment showing how to omit it (and drop the imports) if you don't want attachment search. | guide-content |
| "Define your data model and codecs" (all model/codec/schema snippets) | Presents `Task`/`Project`/`TaskComment`, their codecs, and `schemas.dart` as copyable code blocks. | None of the code blocks include their `import` lines. Codecs reference `KmdbCodec`, `VaultRef` (needs `package:kmdb/kmdb.dart`) and their model class (needs a relative import); `schemas.dart` needs `package:kmdb/kmdb.dart` + `../models/task.dart` for `TaskPriority.values`/`TaskStatus.values`. | minor | yes — a Dart dev adds the obvious imports; the compiler names each missing symbol. | ~3 min | Snippets are extracted as class bodies without file preambles. | Show the `import` header on at least the first codec and on `schemas.dart` (they establish the `package:kmdb/kmdb.dart` + relative-model pattern), or state once "every snippet omits its imports; add `package:kmdb/kmdb.dart` and the relative model import." | guide-content |
| "The full schema set" | The `AppSchemas` aggregate is printed with `static final CollectionSchema tasks = /* the tasks schema shown above */;`. | Copy-pasting the `AppSchemas` block alone does not compile — the reader must physically fold the full `tasks` schema (printed earlier under "JSON Schema admission") into the placeholder. The content is all present, but not assembled in one place. | minor | yes — scroll up, paste the earlier `tasks` schema into the placeholder. | ~2 min | Deliberate de-duplication left a placeholder in the one block a reader would copy wholesale. | Print `AppSchemas` once, complete, with the `tasks` schema inlined (it is the block a reader copies); or make the placeholder a clearly-labelled `// paste the tasks schema from "JSON Schema admission" here`. | guide-content |
| "The companion sample app" / repeated cross-refs (Unlock/Create screen, `sync_service.dart`, `TaskRepository.explainListByProject()`, `lib/src/ui/screens/`) | "Every code sample in this guide is lifted from a real, tested, working application: `packages/kmdb_example_todo/` … If a snippet below looks incomplete, the full context is in that package." Plus ~10 "see the sample app's X" pointers. | `packages/kmdb_example_todo/` is **not present** alongside the guide in this checkout (only `kmdb` + the two extractors ship here). A reader who takes up the guide's offer to "see the full context in that package" for the UI/repository layers finds nothing to open. The data-layer code the guide *walks through* is fully self-contained, so this never blocks the build — but every "see the sample app" pointer is a dead end for a standalone reader, and the guide leans on it for all UI + repository code it never prints. | minor | yes — ignore the pointers; the data-layer sections stand alone without them. | ~5 min (verifying the package really was absent, not mis-pathed) | The guide is coupled to a sibling package for its non-printed (UI/repo) material; that package may not travel with the guide. | Either ship/link `kmdb_example_todo` wherever the guide is published, or soften the "the full context is in that package" promise to only cover material the guide itself prints, and mark the UI/repo pointers as "if you have the example app checked out." | guide-content |
| "Vault: attachments" | Uses `(await db.store.storeInfo()).currentHlc` and `db.vaultStore!` to get the HLC and vault handle. | Both compile and work, but `db.store` is used without ever being introduced — it is the first appearance of the raw `KvStore` accessor, and the guide never says a `KmdbDatabase` exposes `.store`. Discoverable via autocomplete, so non-blocking. | minor | yes — autocomplete on `db.` surfaces `.store` and `.vaultStore`. | ~1 min | An implementation detail (`db.store`) leaks into a snippet without a one-line note. | Add a half-sentence when `db.store` first appears: "`db.store` is the underlying `KvStore`; `storeInfo().currentHlc` gives the current HLC the vault needs." | guide-content |

# Environment / tooling entries (NOT guide defects)

| Item | Detail | Classification |
| :--- | :--- | :--- |
| pub.dev not yet published | The guide's `kmdb: ^0.1.0` (and the two extractors) cannot resolve from pub.dev pre-publication. Consumed via path `dependency_overrides` at `../packages/kmdb`, `../packages/kmdb_extractor_html`, `../packages/kmdb_extractor_markdown`, per the run instructions. | build-environment/tooling (known pre-publication artifact, expected) |
| Native ONNX asset build | First `dart test` builds the `betto_zstd` / ONNX-runtime native assets ("Running build hooks…"). Completed without error; no `install_name_tool`/codesign flake hit on this run, so no `.dart_tool` cleanup was needed. | build-environment/tooling (expected) |

# Outcome

A working to-do **data layer** built end-to-end straight from the guide, with
zero analyzer issues and a green 8-test suite exercising every subsystem the
guide covers (open/close, schema admission incl. the rejection path, CRUD +
queries, the lazy secondary index via `explainedGet`, vault ingest/get + KVLT
packaging, both search surfaces, two-instance authenticated sync convergence,
reactivity, and the full encryption bootstrap/unlock/wrong-passphrase/recovery
fault set). No GUI was launched (out of scope).

**blocker: 0 · major: 0 · minor: 5.** The prior run's blockers are resolved;
the guide now stands alone for the code it asks you to build. All five remaining
entries are `guide-content` and all are `minor`.
