---
title: 0.02_01 Roadmap
subtitle: Mechanics
toc-title: "Contents"
...

# v0.02.01

The latest [Code Review](../reviews/code-review-2026-05-22.md) determined a
number of issues with the codebase before any further feature work commences.
The sections below list out (in order) the work that needs to be done.

## C1 — crash-recovery WAL replay (complete)

Plan:
[plans/completed/plan_crash_recovery_wal_replay.md](../plans/completed/plan_crash_recovery_wal_replay.md)

_(Status: Complete — fixed; recovery now replays retained WALs in full and the
active WAL is no longer deleted. C2 is next.)_

- The fix is **recovery-side only**; the write path (the WAL itself) is correct,
  so no change to how data is logged is needed — recovery just has to stop
  deleting and start replaying the active WAL.
- The trigger surface is **broader than flush**: every `VersionEdit` records
  `logNumber = activeSequence`, so compaction, sync **ingest** (a sync _pull_
  can lose local unflushed writes), and manifest rotation all arm the same bug.
  The plan's regression suite covers each trigger.
- Two combining defects: the `<=`/`<` off-by-one **and** flush markers being
  written before their SSTable is durable. The fix removes marker-based skipping
  in favour of full WAL replay (idempotent under HLC LWW).
- **Testable with the existing in-memory adapter** — the bug is recovery
  deleting genuinely-durable data, which the memory adapter models faithfully.
  So C1 and its tests can land immediately, without waiting on the harness
  below.

## C2 + H1 + M3 — manifest fsync & durability ordering (complete)

Plan:
[plans/completed/plan_manifest_fsync_ordering.md](../plans/completed/plan_manifest_fsync_ordering.md)

_(Status: Complete — manifest now fsynced, `syncDir` called around new files and
the `CURRENT` swap; verified by a fault-injecting storage adapter. Linux
power-loss verification is release check RC-4.)_

- H1 (`syncDir` never called) and M3 (`CURRENT` swap not fsynced) are folded in
  deliberately: fsyncing the manifest is meaningless on Linux unless the
  directory entry is also synced, so the three cannot be correctly fixed apart.
  They share one root cause and one fix.
- Most of the work is **inserting missing `syncFile`/`syncDir` calls** — flush
  and compaction already order _append-before-delete_; the only genuine
  re-ordering hazard is manifest rotation (publish `CURRENT` only after the new
  manifest is durable, else a crash loses the entire level map). No
  `StorageAdapter` interface change is needed.
- **Caveat — this plan cannot use the in-memory adapter.** Its
  `syncFile`/`syncDir` are no-ops and a "crash" never loses buffered data, so
  the entire C2/H1/M3 bug class is invisible to it. The plan therefore makes
  **building a `FaultyStorageAdapter`** (the fault-injection harness from §8)
  its Step 0 — modelling pending-vs-fsynced content, directory-entry durability,
  and a `crash()` that discards un-synced state. This harness is the gating
  dependency for all durability testing and is reusable for C1's mid-flush case
  and any future ordering work; each C2 regression test is specified to be
  confirmed _failing before the fix, passing after_.
- One accepted cost: an extra manifest `syncFile` plus one or two `syncDir`
  calls per flush/compaction/ingest. Marginal against the fsyncs already on
  those paths; can be batched later if profiling demands.

## H3 — vault GC / recovery fail-safe ref counts (complete)

Plan:
[plans/completed/plan_vault_gc_failsafe.md](../plans/completed/plan_vault_gc_failsafe.md)

_(Status: Complete — all ref-count readers now share one fail-safe helper
(`VaultRefCount.read`); deletion requires a positive determination of zero
references and an undecodable counter is retained. The stub-orphan sync-ordering
risk below remains a separately-scoped follow-up.)_

- Investigation found the ref count is decoded in **three** places: the
  interceptor already uses the real `ValueCodec` (correct), while `VaultGc`
  **and** `VaultRecovery` each hand-roll their own CBOR micro-parser that
  returns `0`/"no ref" on any surprise — and both then permanently delete.
  `VaultRecovery._hasKvRef` is worse: it `catch (_) => false`, so a decode
  _exception_ also reads as "unreferenced," and this runs on every unclean open.
- The fix unifies all readers on **one fail-safe helper** and inverts the
  default: deletion requires a positive determination of zero references (absent
  counter, which the protocol guarantees means zero, or a decoded
  `refCount == 0`); an **undecodable** counter is treated as _referenced_ and
  retained. Removes ~120 lines of duplicated hand-rolled CBOR.
- **Coordinates with document versioning**
  ([plans/plan_document_versioning.md](../plans/plan_document_versioning.md)),
  which edits the same files and adds `$ver:` entries as new ref sources with
  compaction-time decrements. H3 should land first so versioning reuses the
  shared helper instead of adding a fourth decoder; the versioning plan's "scan
  `$ver:` for refs" wording should be reconciled to the counter-based model.
- Flags a **related, separately-scoped** risk: `VaultRecovery` deletes "manifest
  present, no ref" objects as orphans, which also matches a freshly-synced stub
  whose referencing document hasn't arrived yet — a sync-ordering data-loss now
  tracked as **H3-FU** below.

## H3-FU — vault stub-orphan recovery (sync-ordering, complete)

Plan:
[plans/completed/plan_vault_recovery_stub_orphan.md](../plans/completed/plan_vault_recovery_stub_orphan.md)

_(Status: Complete — fixed at the **producer**, not in recovery.
`VaultStore.createStub` now reads `$vault:{sha}` via the fail-safe
`VaultRefCount.read` and refuses to write a ref-less manifest, enforcing the
spec §24 invariant that a stub always has a positive KV reference. The H3
fail-safe rule extends here too: an undecodable ref is treated as referenced.
Recovery code is unchanged beyond removing the resolved `TODO`. The
investigation also corrected two prior misreadings: `$vault` entries **do**
propagate to peers via SSTable ingest (no namespace filter is actually
applied), and `syncVaultMetadata` has no production callers today, so the
hazard was latent — making this the right moment to land the contract,
before the peer-side stub orchestration is wired up.)_

## H5 — sync lease CAS atomicity (complete)

Plan:
[plans/completed/plan_sync_cas_atomicity.md](../plans/completed/plan_sync_cas_atomicity.md)

_(Status: Complete — `SyncStorageAdapter.providesAtomicCas` declared
per-instance; `LocalDirectoryAdapter` gained an opt-in `atomicCas` mode that
uses `File.create(exclusive: true)` + an `fcntl` advisory lock;
`ConsolidationCoordinator` skips consolidation (loss-free) when the adapter is
non-atomic. Reusable conformance + 32-way contention regression guard in
`test/support/sync_adapter_conformance.dart`.)_, plus its testing companion
**harness mixed-storage + behavioural cloud simulation** →
[plans/plan_harness_mixed_storage.md](../plans/plan_harness_mixed_storage.md)

- Investigation reframed H5: CAS atomicity is a property of the _backend's
  consistency model_, not just adapter code — the same `LocalDirectoryAdapter`
  can be atomic on a real disk (via `O_EXCL` / advisory locks) but **cannot** be
  on a cloud-synced folder. So the fix is a written CAS contract, an honest
  per-adapter atomic-CAS capability, and a coordinator that **skips
  consolidation** (loss-free, since it is only an optimisation) when atomicity
  isn't guaranteed — backed by a reusable conformance/**contention** test every
  adapter must pass.
- These two plans directly answer the review's §8 testing gap: H5 ships the
  adapter contention test; the harness plan adds per-device adapters, a
  `CloudProfile` abstraction, and a **behavioural cloud-API simulator**
  framework (fake `http.Client` driving the _real_ adapter) so eventual
  consistency and CAS atomicity are tested deterministically in CI, with real
  services reserved for pre-release. The framework is **general across
  providers** (Drive, Dropbox, iCloud, …).
- Sequenced **H5 → harness mixed-storage → `plan_google_drive_sync.md`**; the
  Drive plan was updated to depend on both, ship a Drive simulator +
  `CloudProfile`, and verify the open atomicity risk that **Drive permits
  duplicate filenames**, so a name-keyed lease create may not be exclusive.

## H2 — atomic `WriteBatch` (complete)

Plan:
[plans/completed/plan_writebatch_atomicity.md](../plans/completed/plan_writebatch_atomicity.md)

_(Status: Complete — the WAL now carries a new `0x04` batch frame; every
`WriteBatch` lands as one frame with one checksum, one append, and one fsync,
and recovery drops the entire frame on truncation. Meta-writes (dirty flag,
generation counter, namespace registry) are folded into the same frame so a
single user write is one atomic unit. The legacy individual-record format is
still decoded for back-compat.)_

- Confirmed the batch was non-atomic two ways: across a crash (each entry was a
  separate, separately-fsynced WAL record with no group boundary) **and**
  in-process (the loop awaited between memtable mutations, so a concurrent
  `get()` could see a half-applied batch). The surrounding generation-counter
  and namespace-registry writes were also _separate_ engine ops, so a single
  user write was up to four un-grouped WAL records.
- Fix is a single **batch frame** (one checksum, one append, **one fsync**) +
  synchronous memtable application — the batch is all-or-nothing and, as a
  bonus, N fsyncs collapse into one. Meta-writes were folded into the same
  frame so a user write is one atomic unit, and the WAL decoder stays
  back-compatible with legacy individual records.
- Fully **CI-tested** with the in-memory adapter (truncate the frame →
  recovery drops the whole batch); no §28 release-checklist entry needed.

## H4 — compaction space reclamation (PR1: version collapse + policy hook) (complete)

Plan:
[plans/completed/plan_compaction_reclamation.md](../plans/completed/plan_compaction_reclamation.md)

_(Status: Complete — PR1 of 2 ([#24](https://github.com/bettongia/kmdb/pull/24)
merged). Compaction now collapses superseded versions per `(namespace,
userKey)` group via a streaming reclamation transform, with a
per-namespace-class policy hook exempting `$ver:`. Tombstones are still
retained verbatim — the follow-up **H4-FU** below adds conditional
tombstone GC.)_

- Investigation split this into two operations with very different safety, which
  the review's one-line fix had conflated. **Version collapse** (drop superseded
  versions per key) is safe at _any_ compaction level because reads re-merge all
  levels — a large, low-risk win. **Tombstone GC** is _not_: it is only safe in
  an all-levels compaction (KMDB levels do not imply recency, so "bottom level"
  is insufficient) **and**, in a synced database, only past a sync horizon —
  otherwise dropping a tombstone lets a late-syncing peer resurrect deleted
  data. The two operations are separately reviewable and ship as two PRs.
- **D1 was revised from the recommendation** during sign-off: the
  "drop-immediately when local-only" fast-path is **not safe** because a
  previously-local DB that has already GC'd tombstones can resurrect deleted
  data the first time sync is enabled. The confirmed rule is: synced →
  `min(currentHlc)` across HWMs; local-only → `now - tombstoneGraceDuration`
  (wall-clock, with a grace window that protects the local → synced
  transition).
- It is the **general reclamation framework** the document-versioning feature
  plugs into: `$ver:` namespaces are exempt from collapse and use a keep-N /
  retention policy instead. That feature's plan was hardened in lockstep —
  dependencies on H2 (atomic `$ver:` writes), H3 (fail-safe vault refs), and H4;
  a soft-delete model (delete recorded as a `$ver:` version); and an explicit
  **full-drain guarantee** (keep-N is a floor for _live_ docs only, so deleted
  documents reclaim to zero residue rather than leaking storage).
- Core no-resurrection case is **CI-testable** (drop a tombstone, ingest a
  crafted older-HLC SSTable, assert no resurrection); the multi-device case is
  covered by the harness once `plan_harness_mixed_storage.md` lands and is
  tracked from PR2's plan as a release-checklist item.

## H4-FU — safe tombstone GC (sync-horizon-gated) (complete)

Plan: [plans/completed/plan_tombstone_gc.md](../plans/completed/plan_tombstone_gc.md)

_(Status: Complete — PR2 of 2 ([#25](https://github.com/bettongia/kmdb/pull/25)
merged). The single-file `_compactAll` path now drops a group's surviving
tombstone when `allLevels && tombstoneHlc < horizon`, with horizon =
`min(currentHlc)` across HWMs (synced) or `now - tombstoneGraceDuration`
(local-only, default 7 days). Partial compactions retain tombstones
verbatim. `$ver:` history-bearing namespaces remain exempt.)_

- Drops a group's surviving tombstone only when both safety conditions hold:
  the compaction covers **all levels** that could hold an older version (in
  practice `_compactAll` / single-file collapse), **and** the tombstone's HLC
  is below the GC horizon (`min(currentHlc)` across HWMs when synced;
  `now - tombstoneGraceDuration` when local-only).
- **Known limitation surfaced for separate follow-up:** strict `min(currentHlc)`
  is pegged by the slowest device, so a dead/inactive peer blocks GC forever.
  PR2 documents this and ships the conservative default; an eviction rule
  (max device staleness) is intentionally deferred to keep PR2 reviewable.
- **Multi-device "no resurrection" scenario** is tracked in two places (no
  standalone plan): a release-checklist entry registered by PR2 in
  `docs/spec/28_release_checklist.md`, and as a Step 5 scenario item in
  [plans/plan_harness_mixed_storage.md](../plans/plan_harness_mixed_storage.md)
  so the harness work delivers it when that plan lands. CI coverage of the
  core invariant uses an in-process ingest test in PR2 itself.
- Implementation model: Opus / strong-model review — easy to get *almost*
  right and silently resurrect deleted data.

## H4-FU2 — tombstone GC: stale-device eviction

Plan: [plans/plan_tombstone_gc_stale_eviction.md](../plans/plan_tombstone_gc_stale_eviction.md)

_(Status: Open — explicitly deferred from H4-FU during PR2 sign-off.
Documented as a known limitation in
[plans/completed/plan_tombstone_gc.md](../plans/completed/plan_tombstone_gc.md)
and [spec/12_sync.md](../spec/12_sync.md).)_

- `HighwaterMark.minCurrentHlcAcrossDevices` (PR2) returns the strict
  `min(currentHlc)` across every `.hwm` file. The horizon is therefore
  pegged by the slowest reporter — a peer that goes offline and never
  returns blocks tombstone GC indefinitely.
- A stale-device policy similar to the one historically described in
  `12_sync.md` (90-day timeout, requiring full re-sync to rejoin) is the
  expected shape: exclude HWM files whose `lastUpdated` is older than
  the threshold, allowing the horizon to advance. The threshold needs to
  be ≥ the longest realistic offline window for a legitimately-active
  device, with a safe-default value documented.
- Returning devices that exceeded the threshold must perform a **full
  re-sync** rather than incremental catch-up; otherwise they can deliver
  records older than the now-advanced horizon, defeating PR2's safety
  argument. The interaction with the existing `peers` map in HWM files
  and with `isPeerStale` (currently Phase-5 stubbed) needs design work.
- Worth pairing with **H4-FU3** below — together they form the
  "tombstone GC robustness" follow-up cluster.

## H4-FU3 — tombstone GC: ingest-side horizon floor

Plan: [plans/plan_tombstone_gc_ingest_floor.md](../plans/plan_tombstone_gc_ingest_floor.md)

_(Status: Open — explicitly deferred from H4-FU during PR2 sign-off.
The plan's "no resurrection across sync (CI-testable)" Step 5 test was
annotated as deferred for this reason; the cross-device version is
[RC-6](../spec/28_release_checklist.md#rc-6--multi-device-tombstone-non-resurrection).)_

- PR2's safety argument rests on the **distributed invariant** that no
  peer can produce an SSTable containing records below the GC horizon,
  because every peer's `currentHlc >= horizon` by construction of
  `min(currentHlc)`. The invariant is sound in a well-behaved sync
  topology but **not enforced locally** — a crafted (or stale) ingested
  SSTable carrying older records bypasses tombstone protection because
  the local engine has no record that those records' HLCs were ever
  GC-eligible.
- A local floor would persist the highest horizon ever used for
  tombstone GC (e.g. in `$meta` under `gc:tombstoneFloor`) and have
  [`ingestSstable`](../../packages/kmdb/lib/src/engine/kvstore/kv_store.dart)
  reject (or filter) records whose HLC is strictly below it. Decisions
  needed: reject the whole SSTable, or filter record-by-record (more
  expensive but tolerates mixed-HLC files); error vs. silent skip;
  whether the floor needs to be replicated alongside HWM data so peers
  can recover. None of these are obvious — investigation should precede
  implementation.
- Closes the loop on PR2's CI test: with a floor in place, the
  "construct an older-HLC SSTable and ingest it after GC" scenario
  can assert no resurrection deterministically, without waiting for
  the multi-device harness.

## M1 — SSTable reader caching

Plan:
[plans/plan_sstable_reader_cache.md](../plans/plan_sstable_reader_cache.md)

_(Status: Investigated)._

- The fix splits cleanly: a **table cache** of open readers (footer + index +
  filter) is the high-value, **format-change-free, backward-compatible** part —
  it turns the whole-file hash from a per-_read_ cost into a one-time per-_file_
  cost. "Footer-only cheap open" is deferred to a second phase because the
  footer/index/filter are currently covered only by the whole-file hash, so
  skipping it would need per-section checksums (a format change).
- Distinct from the §15 query-layer cache; named `TableCache` to avoid
  confusion. Eviction is tied to level-map mutations (flush/compaction/ingest/
  rotation/rename). Benchmark-measured; no §28 entry needed.

## M2 — real UTF-8 namespace encoding

Plan:
[plans/plan_utf8_namespace_encoding.md](../plans/plan_utf8_namespace_encoding.md)

_(Status: Investigated)._

- Investigation found the namespace is encoded to bytes in **three divergent
  paths** (`KeyCodec._toUtf8`, `WalRecord._toUtf8`, and `LsmEngine`'s prefix
  builders calling `codeUnits` directly) that must be unified on one UTF-8
  helper or non-ASCII scans silently miss. Keys are ASCII UUIDv7 hex and are
  unaffected — only namespaces (collection names) carry the bug.
- **Backward-compatible by construction:** ASCII namespaces are byte-identical
  under `utf8.encode`, so every working database is unchanged (no migration);
  only the currently-corrupted non-ASCII case changes. The one new design choice
  is NFC normalisation of namespaces at the boundary (recommended).

## §6 code & doc tidy-up

Plan: [plans/plan_code_doc_tidyup.md](../plans/plan_code_doc_tidyup.md)

_(Status: Investigated)._

- Clears the §6 dead-code / doc-drift items in one small PR. Two carry
  subtleties flagged in the plan: the `LsmEngine.get()` `found` flag must keep
  its three-state (absent vs tombstone vs hit) semantics — collapsing it to a
  plain `Uint8List?` would resurrect deleted data — and the manifest-rotation
  metadata loss is documented now, with the proper fix (track `SstableMeta`)
  deferred as it exceeds tidy-up.
- (The earlier §26 spec-number collision is avoided going forward by a
  convention in `plans/README.md`: spec section numbers are assigned at creation
  time, not pre-reserved in plans.)
