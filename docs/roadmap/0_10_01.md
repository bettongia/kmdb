---
title: 0.10.01 Roadmap
subtitle: Trust the boundary, not the folder
toc-title: "Contents"
...

# v0.10.01

## Purpose

This is a **hardening track**, following the precedent set by
[0_02_01](completed/0_02_01.md) — the roadmap that closed out the durability
findings of the 2026-05-22 code review. It exists to work through the findings
of the **2026-07-18 release-readiness review**
([docs/reviews/release-readiness-review-2026-07-18.md](../reviews/release-readiness-review-2026-07-18.md)),
which recommended **against tagging `0.1.0`** in the codebase's current state.

> ### ⚠️ Callback to the review — do not close this track early
>
> **The review is the authoritative status board for this work.** Its §9
> "Coverage actually achieved" records exactly which workstreams ran and to what
> depth. As of 2026-07-20:
>
> | Workstream | State |
> | :--- | :--- |
> | W2 security, W3 FFI | ✅ Complete |
> | **W4** concurrency/durability | ✅ **Complete** (2026-07-20) — found **D-1**; confirmed the 0.02.01 hardening held |
> | **W1** spec conformance | ⚠️ **~40%** at the time of this snapshot — tiers 1–3 audited, **tiers 4–6 never traced** (see WI-1). **Since completed: WI-1 finished the sweep (§13 in pass 3) and is marked ✅ Complete below.** |
> | W5 CLI, W6 code health | ⚠️ **Partial** — see §9 for what was never exercised |
>
> **Every item in the review must be worked through before `0.1.0` is tagged,
> not merely the criticals closed by PR #61.** The review's §6 findings register
> is the checklist; its §10 groups findings by shared remediation work.
>
> **The specific trap this callback exists to prevent:** W1 found a spec section
> marking a feature **Required on mobile and web** that does not exist at all
> (SC-3), and two specs giving **contradictory byte layouts** for a format
> `0.1.0` freezes (SC-2). Both were in the 40% that got looked at. The untraced
> 60% has had no such scrutiny.

## Work Item Tracking

Ordered by **priority**. `WI-0`, `WI-11`, `WI-12`, `WI-13` and `WI-14` are
merged, `WI-1` (the W1 sweep) is complete, `WI-2` (spec corrections) is
complete, `WI-3` (value AAD) is **complete**
([PR #68](https://github.com/bettongia/kmdb/pull/68), merged 2026-08-09), and
`WI-6` (smaller independents, incl. C-1) is **complete**. `WI-4` (sync
authentication) is **complete** — the `SecretStore` precursor
([PR #73](https://github.com/bettongia/kmdb/pull/73)) and the crypto core
([PR #74](https://github.com/bettongia/kmdb/pull/74), `kmdb-qa` PASS, merged
2026-08-10). Prerequisites are in the dependency map below — **the ordering is
not arbitrary and WI-9 must be last**.

| WI | Pri | Title | Status | Plan / detail |
| :-- | :-: | :--- | :--- | :--- |
| **WI-0** | — | Sync trust boundary — validation & robustness (S-1, S-3, S-4, S-6, S-7, D-1) | ✅ **Complete** | [PR #61](https://github.com/bettongia/kmdb/pull/61) · [plan](../plans/completed/plan_0_10_01_sync_trust_boundary.md) |
| **WI-11** | **1** | 🔴 **SC-10 + SC-15 — the planner trusts predicates the index cannot answer** (scope covers **secondary-index, FTS, and Vec** state — all three carried the identical SC-10 defect) | ✅ **Complete** (2026-07-23) — all 5 phases; `kmdb-qa` signed off after the §16/§20 spec correction and test hardening | [PR #62](https://github.com/bettongia/kmdb/pull/62) · [plan](../plans/completed/plan_0_10_01_index_predicate_trust.md) |
| **WI-12** | ↔ | SC-5 — retire the `$meta` `device_id` copy (authoritative store is already the local `DEVICE_ID` file) | ✅ **Complete** | Full deletion (read *and* write) of the `$meta` `device_id` path — greenfield project, no legacy databases to migrate. [PR #63](https://github.com/bettongia/kmdb/pull/63) · [plan](../plans/completed/plan_0_10_01_device_id_meta_retire.md) |
| **WI-13** | ↔ | `gen:{ns}` generation-counter classification (cache correctness) | ✅ **Complete** — moved to local-only `$$genstate`; ingest-side bump + per-namespace `writeEvent` added to `LsmEngine.ingestAt0` (also closes a `watch()`/`stream()` cross-device reactivity gap); `$cache` reclassified to `$$cache` | [PR #66](https://github.com/bettongia/kmdb/pull/66) · [plan](../plans/completed/plan_0_10_01_gen_counter_classification.md) |
| **WI-14** | ↔ | Dirty-open flag (`dirty`) syncs via `$meta` under a device-independent key — same SC-10/Q-D shape | ✅ **Complete** (merged 2026-07-29) — moved to local-only `$$dirtystate`, mandatory cross-device LWW-erasure regression added and verified to fail on revert | [PR #65](https://github.com/bettongia/kmdb/pull/65) · [plan](../plans/completed/plan_0_10_01_dirty_flag_meta_retire.md) |
| **WI-1** | — | Complete W1 spec conformance | ✅ **Complete** | Three passes; §13 audited 2026-07-20 (pass 3 produced SC-15/SC-17…SC-20) |
| **WI-2** | **2** | Spec corrections — SC-2, SC-3 (**5 sites**), SC-11…SC-20 | ✅ **Complete** — all spec SC corrections landed (batches `cfffc67`, `b469663`, `8dff7ec`, `cc843d7`); SC-16 spun out as its own code fix ([PR #67](https://github.com/bettongia/kmdb/pull/67)); the last residual (`vec_index_state.dart:119` doc-comment → `$$vecstate`) fixed 2026-08-06 | **SC-17 done** (unbalanced fence closed, batch 1). The §16 *false `$`-prefix upload-filter* claim is also **corrected** (batch 1 — §16:236/244 now describe the real `$$`/`.local.sst` mechanism). Scoping note: the §16/§20 sentences that placed index/FTS/Vec state in `$meta` were corrected by **WI-11** (→ `$$indexstate`/`$$ftsstate`/`$$vecstate`, 2026-07-23), not by WI-2 |
| **WI-3** | 4 | E-2 value AAD — bind ciphertext to its context | ✅ **Complete** (merged 2026-08-09) — required `ValueContext` AAD binds `(namespace, key)`; format bump 1→2; kmdb-qa PASS; found a 6th encrypted site + 81 kmdb_cli sites during impl | [PR #68](https://github.com/bettongia/kmdb/pull/68) · [plan](../plans/completed/plan_0_10_01_value_aad.md) |
| **WI-4** | 5 | T1 sync authentication — SecretStore, envelope, enrollment | ✅ **Complete** — `SecretStore` precursor ([PR #73](https://github.com/bettongia/kmdb/pull/73), also closes **C-1**, WI-6) + the crypto core (`SyncAuthenticator`, six-class HKDF, `SyncAuthEnvelope`, the `SyncAuthenticatingAdapter` decorator, Q1 quarantine composition, vault manual threading, `remote pair` enrollment) merged ([PR #74](https://github.com/bettongia/kmdb/pull/74), kmdb-qa PASS, merged 2026-08-10). | [sync_auth](../plans/completed/plan_0_10_01_sync_authentication.md) (core) |
| **WI-5** | 6 | SC-1 unlock policy — wrapped-copy DEK model, biometric, re-auth | ✅ **Complete** (merged 2026-08-22, [PR #75](https://github.com/bettongia/kmdb/pull/75)) — `DekCache` removed; authenticated wrapped-DEK unwrap closes SC-1; added third biometric-wrapped DEK (per-device local `SecretStore` state), `KEKSource`/`ReauthPolicy` (default-on 14-day interval), `KmdbDatabase.open(secretStore:)` + `enableBiometricUnlock`/`disableBiometricUnlock`/`lock()`, `kmdb_flutter` `BiometricKekProvider`, headless/server hook; `dbScopedSecretKey` promoted to core. Built in 6 committed phases; CLI session agent split out. CI pinned to Dart 3.12.2 / Flutter 3.44.4 + lcov-2.0 fix landed with this PR (3.13.1 adoption deferred — see Follow-ups, now ✅ **complete**, 2026-08-23). | [unlock_policy plan](../plans/completed/plan_0_10_01_unlock_policy.md) · [cli_session_agent](../plans/plan_0_10_01_cli_session_agent.md) (split) · [proposal](../proposals/unlock_policy.md) |
| **WI-6** | 7 | Smaller independents — C-1, C-2, L-1, S-5, S-8, F-1 | ✅ **Complete** — C-1 done ([PR #73](https://github.com/bettongia/kmdb/pull/73), [plan](../plans/completed/plan_0_10_01_secret_store.md): core `SecretStore` + profile-dir `DirectorySecretStore`); C-2/L-1 done ([PR #69](https://github.com/bettongia/kmdb/pull/69)); S-5 done ([PR #70](https://github.com/bettongia/kmdb/pull/70)); S-8 done ([PR #71](https://github.com/bettongia/kmdb/pull/71)); F-1 → WI-8 (external repos). | — (see below) |
| **WI-7** | 8 | QA advisories — A2, A3, A5, A6 | ✅ **Complete** — A3 done ([PR #72](https://github.com/bettongia/kmdb/pull/72), kmdb-qa PASS, merged 2026-08-10); A2 ✅ already done in WI-0; A5 → **WI-10** (CHANGELOG sweep); A6 → **RC-3** (Windows verification) | [A3 plan](../plans/completed/plan_0_10_01_a3_quarantine_reporting.md) |
| **WI-8** | 9 | `betto_zstd` frame-size cap — **closes S-2's second half** | ✅ **Complete** — `betto_zstd 0.1.0-dev.4` shipped the pre-allocation `maxOutputBytes` cap (external repo), and KMDB now consumes it (`ValueCodec.decode` passes `kMaxDecodedValueBytes`) — **S-2 fully closed** | External repo · [plan](../plans/completed/plan_0_10_01_s2_kmdb_decompress_bound.md) |
| **WI-9** | **last** | Release dependency gate — 12 packages to `0.1.0` | 🔄 **In progress** — Phases A (deps published), B (promotion + override removal, [PR #84](https://github.com/bettongia/kmdb/pull/84)), and C (W6 sweep + spec-auditor) done; **release-ninja review done** (2026-09-02): #1 web-compile fixed ([PR #86](https://github.com/bettongia/kmdb/pull/86)), #4 publish dry-run gate fixed ([PR #87](https://github.com/bettongia/kmdb/pull/87)), #5 README/§19 reconciled, #2 SAHPool barrel-export fixed ([PR #88](https://github.com/bettongia/kmdb/pull/88)), **#3 kmdb_icloud macOS CI build deferred to [v0.2.0](0_20.md)**; **remaining:** §28 RC-items, then Phase D tag | ⚠️ **Must be last** — see below |
| **WI-10** | ↔ | W5/W6 remainder — untested CLI commands, code-health checks | ✅ **Complete** — W5 and W6 both done | Review §9. **W5 done:** [quick wins plan](../plans/completed/plan_w5_cli_coverage_quick_wins.md) ✅ · [vault search CLI plan](../plans/completed/plan_w5_vault_search_cli_coverage.md) ✅; follow-up defect [`builtAt` stamping plan](../plans/completed/plan_index_builtat_stamp.md) ✅ (IndexManager now stamps `builtAt` on build completion, at parity with FTS/Vec). **W6 done (2026-08-26)** — public-API/dead-code/doc-comment audit + CHANGELOG (incl. A5) via the `kmdb-qa` full-codebase sweep, fixed and merged in [PR #85](https://github.com/bettongia/kmdb/pull/85); the paired `kmdb-spec-auditor` truth pass landed its fixes as `c0b8666`. Ran as WI-9 Phase C, per the re-sequencing. |

## Dependency Map

```
WI-1 (complete W1 sweep)  ← do FIRST: everything else is scoped on an
  │                          incomplete picture until this lands
  ├── WI-2 (spec corrections)   ← the sweep may add to SC-2/SC-3's list
  └── may add findings to any WI below

WI-2 (spec corrections)
  └── SC-3 needs a DECISION (build the cache, or correct §15) before work starts

WI-3 (value AAD)  ─┐
WI-4 (sync auth)  ─┤
WI-5 (unlock policy) ─┤   all three change formats, APIs, or betto_* packages
WI-6 (independents)  ─┤   → every one of them must land BEFORE WI-9
WI-7 (QA advisories) ─┤
WI-8 (betto_zstd)  ──┘

WI-4 (sync auth)
  └── design session DONE (2026-08-10) — all crypto decisions resolved across
      two reviewer passes. Split into a SecretStore precursor
      (plan_0_10_01_secret_store.md — PR #73, merged; also closed C-1/WI-6)
      plus the crypto core (plan_0_10_01_sync_authentication.md) — A3
      quarantine reporting (its Q1 prerequisite) merged (PR #72), and the
      crypto core itself is now implementation-complete on branch
      `20260810_plan_0_10_01_sync_authentication`, awaiting kmdb-qa
      sign-off and PR.

WI-5 (unlock policy)
  └── BLOCKED on proposal §9 — agent DEK lifetime, WebAuthn PRF support,
      default interval, per-operation escalation scope

WI-8 (betto_zstd)  ← also one of WI-9's 12 packages, so it must land first
                      or its 0.1.0 gets burned and republished

WI-9 (release gate)  ← LAST. Publishing 0.1.0 of 12 packages and then needing
                       a change means burning those version numbers.
```

**Why WI-1 is first:** it is hours of work, and until it lands every other
estimate is made on 40% visibility. W1 has already found one "spec marks a
feature Required that doesn't exist" — the cheapest possible moment to find the
next one is before scoping, not during.

**Why WI-9 is last:** it is the only irreversible step. Everything else can be
redone.

> **D-1** (the vault-indexing isolate lifecycle finding from W4) was folded into
> WI-0's plan (Phase 3b) and is closed by PR #61.

> ### ✅ S-2 is now fully closed (both halves landed)
>
> Raised by `kmdb-qa` on 2026-07-20 during the PR #61 review as only partially
> closed by WI-0, and recorded here because WI-0 would otherwise read as
> closing S-2 outright.
>
> The KMDB-side cap (`ValueCodec.kMaxDecodedValueBytes`, 1 MiB) originally fired
> **after** `decompress()` returned. That bounded a survivable-but-oversized
> value, and the per-document guards in `dump`/`export`/`scan` mean one
> poisoned value can no longer abort a whole collection — both real
> improvements, but not S-2's headline attack: a Zstd frame *declaring* a huge
> decompressed size still reached `malloc` unbounded, because that check
> belonged upstream in `betto_zstd` (a separate repo).
>
> **Both halves have now landed.** `betto_zstd 0.1.0-dev.4` added a
> pre-allocation `decompress(data, {int maxOutputBytes})` that rejects an
> over-limit frame — throwing `ZstdLimitExceededException` — **before**
> allocating the destination buffer (WI-8). KMDB's own
> `plan_0_10_01_s2_kmdb_decompress_bound.md` then bumped the `betto_zstd` pin
> and threaded `kMaxDecodedValueBytes` through `ValueCodec.decode`'s two
> `decompress` call sites (plus a third call site found during that work,
> `VersionEntry.decodeIsDeleteSync`), mapping the betto_zstd exception onto
> `DecodedValueTooLargeException` so the exception surface `decode` promises
> stays consistent. `docs/spec/05_value_encoding.md`'s "Decompressed-Size
> Bound (S-2)" section has been updated to describe the pre-allocation reject.
> **S-2 is fully closed.**

## Threat-model decision (Group C) ✅ Decided

The review's **E-1** established that `docs/spec/31_encryption.md` documents a
**passive-only** threat model — a provider who *reads* your data — and never
claims the synced data is authentic. Five of the six critical findings follow
from that single gap.

**Decision (2026-07-19): close the gap before `0.1.0`, scoped to T1.**

- **T1 (untrusted provider, read *and* write) — closed in this track.** All sync
  artefacts are authenticated. The provider cannot forge SSTables, vault blobs,
  leases, or high-water marks. The realistic threat this defeats is **account
  compromise** — a phished Google account or stolen OAuth token yields write
  access to the sync folder, and neither full-disk encryption nor the provider's
  own at-rest encryption helps there.
- **T3 (malicious peer) — designed, deferred.** A compromised peer already holds
  the DEK and full plaintext, so authentication buys attribution and blast-radius
  containment, not confidentiality. It needs per-device asymmetric identity,
  enrollment, and revocation. Captured as a proposal, not built here.

**Authentication is decoupled from encryption.** Encryption stays optional and is
sold on its own merits. The reasoning: with full-disk encryption and a reputable
provider, KMDB-level encryption is genuinely marginal for many users — so
forcing it as a *prerequisite* for authentication would trade a low-value
requirement for a high-value guarantee. The sync-auth key is also a
fundamentally lower-stakes secret than the DEK: losing it means re-provisioning,
not losing your data, so it needs no passphrase and no recovery code.

## Work items

> Reference detail per item. **The Work Item Tracking table above is the
> ordering** — these sections are in document order, not priority order.

### WI-11: SC-10 — index state syncs, and a second device silently returns nothing 🔴

**Priority 1. Code fix, and it changes a `$meta` key layout — so it must land
before the `0.1.0` format freeze.**

Found by the second W1 pass and **reproduced end to end**:

```
PROBE A final: ScanStrategy.indexScan, 3 rows
PROBE B:       ScanStrategy.indexScan, 0 rows   (expected 3)
PROBE B unfiltered scan: 3 docs
```

The chain, independently verified:

1. §16:229–230 claims *"the sync engine filters out all `$`-prefixed namespaces
   during SSTable upload, so index state never leaves the device."*
   **No such filter has ever existed** — `git log -S` finds it at no commit. The
   only filter is file-level `localOnly`, driven by `isLocalOnly`, which matches
   `$$` **only**. §12:455 correctly documents `$meta` as uploaded; the two specs
   contradict and §12 is right.
2. Index state — including `status` — lives in `$meta`
   ([index_manager.dart:70](../../packages/kmdb/lib/src/query/index/index_manager.dart#L70)).
3. Device B pulls A's `$meta` and inherits `status: current` for an index **it
   never built**. `_nameToKey` is a plain XXH64, so it is device-independent, and
   `builtThrough`/`gen:{ns}` sync together and validate each other.
4. B runs an index scan against its own empty `$$index:*` namespaces and returns
   **zero rows for documents that are present and match** — silently, with
   `QueryPlan` reporting `indexScan`.

It self-heals once B *writes* to that namespace, **so a read-mostly second device
is worst affected** — a phone reading what a laptop writes is exactly the shape.

> **Severity note.** The taxonomy reserves 🔴 for data loss, corruption, or a
> security break, and strictly nothing is lost here. Promoted anyway: a silently
> wrong result on the primary read path, with no error and no workaround, is at
> least as damaging as detectable corruption — and unlike corruption the user has
> no signal that anything is wrong. 🟠 is defensible; it blocks either way.

#### ⚠️ Do not "add the missing filter" — it is incoherent, not merely absent

The namespace rule is:

| Prefix | Meaning | Syncs? |
| :--- | :--- | :---: |
| `$` | System namespace | **Yes** |
| `$$` | System **and local-only** | No — `.local.sst`, never uploaded |

`namespace_codec.dart` states that **"the `$$` prefix is a strict superset of the
`$` prefix"**, and the single-`$` namespaces actually in use are `$meta`, `$ver`,
`$vault` and `$sync`.

**So implementing §16's claimed filter would break sync outright** — it would
exclude `$meta` (device identity, generation counters, the wrapped DEK), `$ver`
(version history) and `$vault` (blob references) from upload. The mechanism §16
describes could never have worked; it is not a filter someone forgot to write.

**The actual defect is that index status is device-local state stored in a synced
namespace.** The fix is to move it — to a `$$` namespace, or to a per-device key
within `$meta` — **not** to filter `$`.

#### SC-15 — the same defect, one layer down (added 2026-07-20)

**A case-insensitive equality filter returns zero rows once an index exists on
the field.** Reproduced; the only variable is whether an index was declared:

```
no index declared  -> rows = 1
index declared     -> rows = 0   strategy=indexScan scanned=0 matched=0
control exact case -> rows = 1   strategy=indexScan
```

`FieldFilter.equalityPredicate` is `_op == _Op.eq ? (_path, _operand) : null`
([field_filter.dart:175](../../packages/kmdb/lib/src/query/filter/field_filter.dart#L175))
— it never consults `caseSensitive`, which is a field on the *same class* four
lines above. So the planner is handed an exact-match predicate, does an index
lookup on the exact token, and the in-memory pass §13:507 promises as the safety
net never runs, because the index "already answered."

**SC-10 and SC-15 share a shape, not a fix** (plan review, 2026-07-21): both are
the planner trusting a predicate it has not established the index can answer —
SC-10 across a device boundary, SC-15 across a case-folding boundary — but their
fixes touch disjoint files (a namespace move vs a one-line predicate gate). They
are bundled because they share one test artefact: the equivalence property that
index-path rows must equal full-scan rows. **The SC-15 fix is deliberately
narrow** (gate `equalityPredicate` on `caseSensitive`, plus a doc-comment
contract); the durable guard against a future non-exact matcher (locale folding,
accent insensitivity, normalisation) reintroducing it is that equivalence test,
not a capability contract — see the plan's Q-A decision.

**Why no test caught it:** `filter_test.dart:173–196` covers `caseSensitive`
thoroughly at the `evaluate()` level, and `index_query_test.dart` covers index
selection thoroughly with exact-case predicates. Each half is well tested and
both score well on line coverage. **The seam between them is untested — no test
anywhere asserts that the index path and the full-scan path return the same
rows.** That equivalence property is the test this WI must add.

#### The broader audit this WI must include

Nobody has audited **which `$meta` entries are device-local facts and which are
replicated state**. Every entry needs classifying, and the answer is not obvious
for several:

| `$meta` entry | Classification | Disposition (resolved 2026-07-21, Phase 3 audit closed 2026-07-23) |
| :--- | :--- | :--- |
| Secondary-index state (`status`, `builtThrough`) | Device-local | **Fixed in WI-11** (Phase 2) — SC-10 → `$$indexstate` |
| FTS index state | Device-local | **Fixed in WI-11** (Phase 2) — SC-10, same defect → `$$ftsstate` (folded in 2026-07-21; a read-mostly device's `search()` returns silent zero results otherwise) |
| Vec index state | Device-local | **Fixed in WI-11** (Phase 2) — SC-10, same defect → `$$vecstate` |
| Tombstone GC floor (H4-FU3) | Device-local | **Fixed in WI-11** (Phase 3) — Q-D: resolved from code (device-local by design, was in synced `$meta` under `gc:tombstoneFloor`; LWW could *lower* the floor → resurrection). Moved to `MetaStore.kGcStateNamespace` (`$$gcstate`) |
| Device identity | Device-local (sole store = local `DEVICE_ID` file) | **Fixed in WI-12** — `$meta` `device_id` path removed entirely (read and write) |
| `gen:{ns}` generation counters | Device-local | **Fixed in WI-13 (2026-08-03)** — Q-C resolved: moved to `MetaStore.kGenStateNamespace` (`$$genstate`); `LsmEngine.ingestAt0` was given a compensating ingest-side bump + per-namespace `writeEvent` so cross-device cache invalidation (and `watch()`/`stream()` reactivity) no longer depends on the removed replicated value |
| `enc:blob` (wrapped DEK) | Replicated | Correct as-is — no change |
| Schema / version-policy / namespaces-registry / format-version | Replicated (expected) | **Verified in WI-11 Phase 3** — expected correct, confirmed against code (`schema_manager.dart`, `version_manager.dart` both write under `$meta` symbolic names with no device-specific data; namespace registry and format-version marker are genuinely shared facts about the dataset, not a device) |
| Dirty-open flag (`dirty`) | Device-local, **found mis-placed** | **Fixed in WI-14 (2026-07-27)** — same defect shape as Q-D: `MetaStore.setDirty`/`appendDirtyFlag` wrote the `dirty` key to synced `$meta`; `_nameToKey('dirty')` is device-independent, so plain LWW let a peer's write or clean-close tombstone override this device's own flag — including a **false-negative** direction (a peer's later-HLC clean close could erase this device's genuine crash marker before this device ever read it, silently skipping the `onIndexRebuildRequired` notification). Moved to `MetaStore.kDirtyStateNamespace` (`$$dirtystate`); a mandatory cross-device LWW-erasure regression reproduces the exact interleaving and was verified to fail with the namespace change reverted |

Four entries are fixed in WI-11 (secondary-index, FTS, and Vec state, plus the
tombstone floor); device identity (SC-5) was fixed in **WI-12** (full deletion
of the `$meta` `device_id` path); the dirty-open flag was fixed in **WI-14**
(moved to `$$dirtystate`). `gen:{ns}` was split out to **WI-13**, since it was
materially larger/riskier than a mechanical namespace move (it was read
cross-device for cache invalidation, so "device-local" alone was not
obviously correct without also deciding a compensating mechanism for the
cross-device behaviour that depended on replication) — resolved 2026-08-03
by moving it device-local and adding a matching ingest-side bump. The
remaining families are classified and verified in Phase 3. **All `$meta`
entries are now resolved; none is left undecided.**

**How it survived is the transferable lesson.** The claim was false the day it
was written. WI-0 later built the real `$$`/`.local.sst` mechanism and rewrote
the *adjacent bullet in the same paragraph*, leaving this one standing on the
rationale it had just invalidated. The section reads as freshly maintained
because in part it is — which defeats every heuristic in the review's §11.1:
not stale prose, not a duplicated definition, not present-tense design intent.

### The `$meta` end-state (shared reference for WI-11 + WI-12 + WI-13 + WI-14)

Four WIs touch `$meta`; this is the target they converge on. The governing
invariant: **`$meta` holds only replicated state — every device-local fact moves
to a `$$` namespace that lands in `.local.sst` and never uploads.** That turns the
`$` vs `$$` prefix rule from a mechanical accident (true by luck, violated by five
entries — SC-10 is what that costs) into a *semantic* rule the next `$meta`
addition has to pass.

> **This is now documented in the spec.** The
> [attribute registry](../spec/03a_attribute_registry.md) (an early, unnumbered
> spec section, added 2026-07-24) is the authoritative, code-anchored home for the
> `$meta` register. **WI-12** (`device_id`), **WI-14** (`dirty`) and **WI-13**
> (`gen:{ns}`, merged PR #66) have all landed, with their registry rows/full
> entries updated — **no `$meta` registry row is left mid-change.**
>
> The WI-2 spec-correction items called out here have since landed:
> (a) §04's Device Identity section and §08 no longer describe `device_id` as
> living in platform secure storage — both now describe the real `DEVICE_ID`-file
> mechanism, are explicit that it is **not** in Keychain/SharedPreferences, and
> carry the registry inbound link (batch `cfffc67`); (b) the registry pass
> corrected a WI-11 miss — §31 had continued to list "index/FTS/Vec state" as
> `$meta` content after WI-11 moved it to `$$` namespaces (now fixed), and
> corrected a false "the one `$meta` encryption exemption is `enc:blob`" claim
> (§31 now documents **two** exemptions — `enc:blob` and `formatVersion`).
>
> **WI-2's last item is now fixed (2026-08-06)** — a **code** doc-comment, not
> spec prose: `vec_index_state.dart:119`'s `toBytes` doc said "for `$meta`
> storage" though the state moved to `$$vecstate` (WI-11); the adjacent doc
> comments (lines 57–58, 225) had been updated but this one was missed. Now
> corrected, so **WI-2 is complete**. (The matching
> `device_id.dart:23` "UUIDv7" item was claimed and fixed by WI-12 instead,
> since that plan already rewrote the whole class doc comment — it is no longer
> WI-2's.)

| Entry (symbolic key) | Written by | Classification | To-be location | Driver |
| :--- | :--- | :--- | :--- | :--- |
| `index:{ns}:{path}` | secondary-index state | device-local | **→ `$$indexstate`** ✅ | WI-11 |
| `fts:{ns}:{field}` | FTS index state | device-local | **→ `$$ftsstate`** ✅ | WI-11 |
| `vec:{ns}:{field}` | Vec index state | device-local | **→ `$$vecstate`** ✅ | WI-11 |
| `gc:tombstoneFloor` | tombstone GC floor | device-local | **→ `$$gcstate`** ✅ (`MetaStore.kGcStateNamespace`) | WI-11 |
| `device_id` | device identity | device-local (sole store is the local `DEVICE_ID` file) | **→ removed from `$meta` entirely** ✅ | WI-12 |
| `gen:{ns}` | generation counters | device-local | **→ `$$genstate`** ✅ (`MetaStore.kGenStateNamespace`) | WI-13 |
| `dirty` | dirty-open flag | device-local, **found mis-placed 2026-07-23** | **→ `$$dirtystate`** ✅ (`MetaStore.kDirtyStateNamespace`) | WI-14 |
| `enc:blob` | wrapped DEK | replicated | **stays** | — |
| `schema:{collection}` + `schema:__registry__` | schema state | replicated | **stays** | — |
| `version:config:{collection}` | version retention policy | replicated | **stays** | — |
| `namespaces` | namespaces registry | replicated | **stays** | — |
| `formatVersion` | format-version marker | replicated | **stays** | — |

**Resulting `$meta`** (now that all four WIs have landed): `enc:blob`,
`schema:*`, `version:config:*`, `namespaces`, `formatVersion` — the state that
*should* be identical on every device that syncs the set. Everything else is
device-local. The clean "purely replicated" invariant now holds in full.

Caveats worth holding:

- **`gen:{ns}` (WI-13, resolved 2026-08-03) needed more than a mechanical
  move.** Unlike the other four device-local entries, its cross-device cache
  invalidation had been resting on `$meta`'s replication — moving it
  device-local without a compensating fix would have silently broken that.
  The fix pairs the move with an ingest-side bump/emit in
  `LsmEngine.ingestAt0`: it scans each ingested SSTable once for its distinct
  namespaces and, for exactly that set, bumps the local `$$genstate` counter
  and emits a per-namespace `writeEvent` — closing both the cache-invalidation
  dependency and, as a bonus, a `watch()`/`stream()` cross-device reactivity
  gap (ingest previously emitted only a generic `$sync` event nothing
  consumed). See the [full registry entry](../spec/03a_attribute_registry.md#genns).
- **The dirty-open flag (WI-14) was found by WI-11's own Phase 3 audit and
  fixed by WI-14 (2026-07-27)** — the audit's entire purpose (Goal 3: classify
  *every* `$meta` entry) paid for itself by finding a sixth instance of the
  SC-10 shape that no earlier review or W1 pass had caught. Its only
  production consumer is `KmdbDatabase.open`'s `onIndexRebuildRequired`
  notification callback (`kmdb_database.dart:438`) — informational, not a
  correctness gate for the index rebuild itself
  (`IndexManager.checkInterruptedBuilds` reads each index's own
  now-device-local `$$indexstate` status directly) — but a false-negative
  read (a peer's clean-close tombstone erasing this device's genuine crash
  marker) still meant the application-level notification was silently
  skipped. Moved to `$$dirtystate`, closing the defect before `0.1.0` freezes
  the format.
- **The tombstone floor's `$$` namespace has no pinned name yet** — unlike the
  three index states, it has no data-namespace sibling to mirror (it is one
  per-device key). WI-11 must pick: its own `$$` namespace or a shared local-meta
  one.
- **`device_id` was already authoritatively device-local — WI-12 was a cleanup, not
  a migration (corrected 2026-07-21, verified against code + the `demodb` instance;
  implemented and closed 2026-07-27).**
  `ensureDeviceId` (`kv_store_impl.dart`) read a **plaintext `DEVICE_ID` file in
  the db root first** (outside `sst/`, never synced) and only fell back to
  `$meta` for backward compatibility; its own doc comment said the file "is
  always preferred over the `$meta` value when both are present." So the
  authoritative identity was *already* outside the synced database. The
  `$meta` copy was written on first open and **did** sync, but was **inert on
  read**. Consequences: (a) **SC-5's severity dropped** — the exposure was
  hygiene/confidentiality (the copy leaked each peer's `device_id` and was dead
  weight), not a wrong-identity correctness bug; (b) **WI-12 removed the
  `$meta` `device_id` path entirely (read *and* write)** — the maintainer's
  2026-07-24 scope decision widened from "stop writing, keep the read-fallback"
  to a full deletion, since this is a greenfield project with no legacy
  databases to migrate; the load-bearing file→naming→manifest chain was left
  untouched; (c) §08's "must not be stored inside the database" is
  **substantially already honoured** by the file — the Keychain/secure-storage
  move remains a *separate optional enhancement* (still WI-2); the
  `device_id.dart:37-38`-era "`$meta` is the sole persistence mechanism" claim
  was rewritten by WI-12 itself, not left for WI-2.

### WI-1: Complete W1 — ✅ **Complete (§13 audited in pass 3, 2026-07-20)**

> **✅ WI-1 is complete.** The third pass audited §13 — the section flagged
> below as the material gap — and produced SC-15, SC-17, SC-18, SC-19 and SC-20,
> all of which were subsequently corrected under WI-2. The historical framing
> below is **retained for context**; it describes the mid-sweep state when §13
> was still untraced.

**Priority 2. Historical: two passes done as of 2026-07-20, §13 then still open —
closed by pass 3.**

| State | Sections |
| :--- | :--- |
| **Audited to depth** | §11, §16, §25, §32 (pass 2); tiers 1–3 (pass 1) |
| **Targeted pass** | §10, §26, §30, §12's system-namespace rule |
| **Surveyed only** | **§13**, §20–23, §29, §33, §19, bulk of §12/§24 |

> **§13 (query API) is the material gap and should not wait for a post-release
> pass.** 584 lines, the public API surface `0.1.0` freezes hardest, and now the
> largest untraced section in the spec. Pass 1 predicted the unaudited remainder
> would hold drift at a similar rate; pass 2 confirmed that and then found a 🔴.

**The symbol sweep is done and is not worth repeating.** It was necessary but
low-yield: 21 of 24 hits were false positives, and **every finding of consequence
was invisible to it** — SC-10's symbols all exist and are spelled correctly. The
methods that actually worked are ranked in the review's §11.1: ask what a false
claim would *cause*, use `git log --diff-filter=A` to rank sections by risk, and
follow delegated capability across package boundaries.

**Original framing (retained for context):**

W1 audited tiers 1–3 (encryption, vault, sync, the on-disk formats, the
durability contract). **Tiers 4–6 were never traced:** §10, §11, §13, §16, §22,
§25, §26, §29, §30, §32, §33. §12 and §24 got targeted passes on
encryption/integrity claims only.

The `kmdb-architect` pass identified that the findings share three mechanical
causes, which makes the remainder cheap to sweep:

1. **Duplicated byte layouts** — the same format documented in two sections that
   then drift (this is SC-2).
2. **Spec-named API symbols with nothing binding them to reality** — §15 names
   `$cache` and `KvStoreConfig.sessionCacheMaxObjects`; **neither exists** (SC-3).
3. **Design intent written in the present tense** — reads as implemented.

**Method:** grep every spec-named symbol, namespace, config field and exception
type against the codebase. That single mechanical pass would have caught SC-3
directly, and is far cheaper than re-reading 6,000 lines of prose.

- [x] Extract every code identifier named in tiers 4–6 and grep it
- [x] Trace surviving normative claims to code + test
- [x] Record findings in the review's §6 register and §11 matrix
- [x] Update review §9 to mark W1 complete

### WI-2: Spec corrections — SC-2, SC-3, SC-11, SC-12, SC-13, SC-14

**Priority 3. Scope grew substantially in W1 pass 2.**

> ⚠️ **SC-3 has five correction sites, not one.** §15 is where it was found, but
> §11 repeats `$cache` and `sessionCacheMaxObjects` as fact (SC-13). Fixing §15
> alone leaves the spec still asserting a feature that does not exist. The full
> site list is in the review's Group G — work from that, not from memory.

**Added by pass 3 (§13), 2026-07-20:**

- **SC-17 🟡 — do this one first.** An unbalanced code fence at
  `13_query_api.md:438` means **"Field Path Syntax" and "Filter DSL" do not
  exist as headings in the published `site/spec.html`** — verified in the built
  HTML, not inferred. ~70 lines including the entire filter-DSL table render as
  syntax-highlighted Dart. It is a one-character fix, and sequencing it first
  makes every other §13 correction reviewable in rendered output.
  *Rated 🟡 by the letter of the taxonomy; its effect — the filter DSL is absent
  from the published spec — is arguably 🟠.*
- **SC-16 🟠** — `insert()` is documented `Future<void>` (actually `Future<T>`)
  and always mints a fresh UUIDv7, ignoring `keyOf`, which makes the documented
  `DocumentAlreadyExistsException` **unreachable**. Combined with
  `RawDocumentCodec.encode` silently stripping `_id` — where §13 states a MUST
  three times — `rawCollection.insert(previouslyReadDoc)` **silently duplicates**
  rather than updating or throwing. Reproduced.
  **Fixed** (see `docs/plans/completed/plan_insert_identity_semantics.md`) —
  spun out as its own code-fix work item per the maintainer's call (a
  behaviour bug, not pure doc drift). Resolved as a key-presence-driven write
  API: `put()` is the canonical upsert (mints on a keyless value, updates on a
  keyed one, now returns `Future<T>`); `insert()` becomes a strict create that
  throws `ArgumentError` on a keyed value instead of silently duplicating;
  `KmdbCodec.keyOf` is now `String?` (covariant-safe for all 26 existing typed
  codecs); `DocumentAlreadyExistsException` removed (no reachable caller —
  create-fail-if-exists is not offered because existence is not globally
  answerable under sync/LWW). §13's `insert` return type and the false
  "rawCollection runs all pipeline layers identically" claim corrected.
- **SC-18 🟡** — the `orderBy('_id')` optimisation does not exist; `KvStore.scan`
  has no `descending` parameter at all. **The same false claim is repeated in
  `kmdb_query.dart:96`**, so correcting only the spec leaves it live in the API
  docs.
- **SC-19 🟡** — `stream()` is annotated "lazy; holds LSM snapshot" and
  contradicted by §13's own prose 40 lines later (the code is eager);
  `count()`'s "avoids decoding" holds only with no filter/orderBy/limit/offset.
- **SC-20 🟢** — `requireFreshIndex()` is undocumented in §13, despite being the
  caller-side mitigation for SC-10.

> Substantial parts of §13 traced **clean**: null and negation semantics,
> `isBetween` inclusivity, `contains` dual dispatch, the three-layer write
> pipeline, and the delete path.

**Added by pass 2:**

- **SC-11** 🟠 — §32's `searchVault` API reference documents an **API that was
  never built**, created by a "Vault search planning" commit *before* WI-3
  implemented it. All four `VaultChunk` field names are wrong. Worse, it is a
  **behavioural** claim: the missing-embedding-model path degrades to lexical
  rather than throwing `StateError` as documented, so a caller writing
  `on StateError` silently gets lexical results.
- **SC-12** 🟡 — §25 under-documents `betto_schema` by **13 enforced keywords**,
  so "unknown keywords are silently ignored" is false for precisely the ones
  people reach for (`multipleOf`, `uniqueItems`, `const`…). §25 never names
  `betto_schema`, which is caret-pinned.
- **SC-13** 🟡 — see the SC-3 note above.
- **SC-14** 🟢 — minor drift across §16, §26, §11, §30.

**Original scope:**

**SC-2 🟠 — two specs give contradictory byte layouts, and the security document
is the stale one.** §31 still documents the vault blob format PR #61 replaced,
*and* still describes the S-4-vulnerable `manifest.encrypted` read gate as
current behaviour. §24 is correct. An integrator reading §31 implements the
wrong format **and** the wrong trust model, for a format `0.1.0` freezes.

- [ ] Make §24 the single authority for the vault blob layout
- [ ] Delete the duplicated layout prose from §31; cross-reference instead
- [ ] Remove §31's stale `manifest.encrypted` gate description

**SC-3 🟠 — needs a decision, not a fix.** §15's Materialised View Cache does not
exist: no read or write of `$cache` anywhere in `lib`, and
`KvStoreConfig.sessionCacheMaxObjects` is absent too. `CacheLayer.scan` defers it
to `KmdbQuery`, which never picked it up — a hand-off that reads as "implemented
elsewhere" from both ends. **§15's tier table marks it Required on mobile and
web.**

> ### ✅ SC-3 decided (2026-07-20): correct §15; do not build it
>
> **Correct all five sites** to state plainly that the materialised view cache is
> **not implemented**, and **drop the "Required" marking** from §15's mobile and
> web tier rows.
>
> Three things drove this:
>
> 1. **The specified design is wrong, not merely unbuilt.** §15 specifies
>    `$cache:{namespace}:{queryHash}` — **single `$`, so it syncs.** A
>    materialised view cache is derived, device-local state by definition;
>    putting it in a synced namespace is exactly the mistake **SC-10** embodies.
>    Building it as written would give device B device A's cached query results.
> 2. **The problem may not exist.** §18 measures `Scan (namespace, 100 results)`
>    at **7.43ms P99**. §15's rationale is that mobile and web processes are
>    killed silently and "must rebuild from `$cache` on cold open" — but the
>    rebuild appears to cost tens of milliseconds, not seconds.
> 3. **Nobody has missed it.** `CacheLayer.scan` has deferred to "the Query Layer
>    (Phase 7, `KmdbQuery`)" since Phase 7 — through the vault, encryption, and
>    search work — and `KmdbQuery` never picked it up.
>
> **Make the correction do work.** Rather than deleting the claim, record the
> evidence that would justify building it later: *measure cold-start scan cost on
> a real mobile device and on web; build only if it exceeds a stated threshold.*
> That keeps the decision revisitable on evidence instead of losing the intent.
>
> **If it is ever built, it goes in `$$cache`, not `$cache`** — decided at the
> same time. Derived, device-local state belongs behind the `$$` prefix so it
> lands in `.local.sst` and never uploads, consistent with `$$fts:`, `$$vec:`
> and `$$index:`. §15's current single-`$` naming must not survive the
> correction, or the next reader takes it at face value.

### WI-3 / WI-4: Sync trust boundary and authentication (Groups A + B + C)

Originally one plan; **split into three on 2026-07-19** after the
`kmdb-plan-reviewer` pass found it too large to hold at `Investigated` (nine
phases, three packages, an external repo, a new subsystem, a new user-facing
UX, and a breaking format change). The three are independent and may proceed in
parallel.

| Plan | Scope | Status |
| :--- | :--- | :--- |
| [plan_0_10_01_sync_trust_boundary.md](../plans/completed/plan_0_10_01_sync_trust_boundary.md) | **Validation & robustness** — parser hardening (S-1, S-3), decompression bounds (S-2), vault integrity (S-4), indexing-isolate lifecycle (D-1), lease validation and staging (S-6, S-7), the hostile-input test corpus | **Complete** — [PR #61](https://github.com/bettongia/kmdb/pull/61) |
| [plan_0_10_01_value_aad.md](../plans/completed/plan_0_10_01_value_aad.md) | **E-2** — bind ciphertext to its namespace/key/type. Breaking format change; wants to land before `0.1.0` freezes the format | ✅ **Complete** — [PR #68](https://github.com/bettongia/kmdb/pull/68) (merged 2026-08-09) |
| [plan_0_10_01_sync_authentication.md](../plans/completed/plan_0_10_01_sync_authentication.md) | **T1** — `SecretStore`, `SyncAuthenticator`, transport envelope, device enrollment | ✅ **Complete** ([PR #74](https://github.com/bettongia/kmdb/pull/74), merged 2026-08-10) |

**Sequencing note.** The hardening plan is worth doing **regardless of how the
threat model lands** — corruption, buggy peers, and malformed PDFs exist without
any attacker — so it is not blocked on the other two. AAD is independent of the
secret store and could land first. Sync authentication was the largest and
least specified of the three at planning time; it is now fully implemented.

> **D-1 was added to the hardening plan after the fact** (Phase 3b), from the W4
> workstream: a dead vault-indexing isolate hangs `close()` *before* the memtable
> flush. It sits there rather than in Group E because S-2 and S-8 supply its
> trigger.

> **R-5 — a consequence worth surfacing early.** Once artefacts are
> authenticated, everything already in an existing sync root becomes
> unverifiable. The operation is **"wipe the sync root and re-push from one
> device"**, not "re-run `remote add`". **Implemented as designed:** an
> un-enveloped legacy artefact raises the same `SyncAuthException` as a
> forged one (see §34's R-5), with a message pointing at `remote pair`. Still
> needs to reach the release notes for the `0.1.0` upgrade guide.

### WI-9: Release dependency gate — MUST BE LAST

The review's **O-1** confirmed the `betto_*` closure is **12 packages**, all
still at `-dev`. Blocking work, but mechanical:

- Publish all 12 at a suffix-free `0.1.0`.
- Add **`betto_abnf`** to `CLAUDE.md`'s package list — it reaches core `kmdb`
  transitively via `betto_schema` and was undocumented (O-1). *(Phase B
  superseded the original "and the workspace `dependency_overrides`" half of
  this item: the `betto_*` override block was **removed**, not extended, so the
  closure now resolves purely from each member package's own `dependencies:`
  — the same path a pub.dev consumer takes.)*
- Reconcile the `kmdb_flutter` / `kmdb_icloud` pins, which sit a dev release
  behind `kmdb` and resolve independently (O-1b).
- Confirm `kmdb_flutter` is covered by a CI lane (O-2).
- **Re-run the full suite against the promoted `^0.1.0` pins before tagging.** A
  green run against `-dev` pins is not evidence about the shipped artefact.
- **Dart 3.13 is a hard floor for the release** — the
  [3.13 adoption](../plans/completed/plan_dart_3_13_adoption.md) closes this
  prerequisite for WI-9. betto_pdfium `0.1.0` (like the published
  `0.1.0-dev.4`) declares a Dart 3.13 language version in its native-asset
  build hook, which a 3.12.2 SDK refuses to compile. Promoting the pin to
  `^0.1.0` thus forces the whole workspace and CI onto Dart 3.13.1 / Flutter
  3.47.x. **Done (2026-08-23):** the workspace and CI toolchain moved to Dart
  3.13.1 / Flutter 3.47.1 together — `betto_pdfium` re-pinned to
  `0.1.0-dev.4`, every kmdb package's `environment: sdk:` bumped from
  `^3.12.0` to `^3.13.0`, and the release-notes Dart 3.13 minimum is captured
  below in the release-notes follow-up.

> `betto_builder_tools` is **not** in the gate — it is a `dev_dependencies` entry
> in all three consumers and never reaches a consumer's resolution.

### WI-6: Smaller independents

Grounding (2026-08-09) showed WI-6 is **not** uniformly "no plan needed" — it
fractured into a trivial CLI bundle (done), two items that outgrew "small" (each
now getting its own short plan), and one that leaves this repo:

- **C-1** ✅ **Done** ([plan_0_10_01_secret_store.md](../plans/completed/plan_0_10_01_secret_store.md)).
  The earlier "superseded by WI-0" closure was inaccurate — the
  controlled-location move it assumed (sync-auth secret → `%APPDATA%\kmdb` /
  `~/.config/kmdb` rather than `{dbDir}/local/`) was a **design decision**
  recorded in `plan_0_10_01_sync_trust_boundary.md` ("closes C-1"), but had
  never actually been implemented — C-1 was reopened on 2026-08-10 once that
  was verified against `main`. This plan is what actually performs the move:
  the core `SecretStore` interface (`packages/kmdb/lib/src/secret/
  secret_store.dart`) plus `kmdb_cli`'s `DirectorySecretStore`
  (`packages/kmdb_cli/lib/src/config/secret_store/`), rooted by default at
  the per-user profile config directory rather than `{dbDir}/local/`. DB
  location no longer affects secret confidentiality on Windows, closing the
  original concern ("warn when `dbDir` is outside the Windows user profile").
  A `credentials prune` command was also added, since the store's root is now
  shared globally across every database on the machine. See
  `docs/spec/33_cli_credential_store.md` for the full design.
- **C-2** ✅ **Done** ([PR #69](https://github.com/bettongia/kmdb/pull/69)) —
  `History.save()` best-effort `chmod 600`s `~/.kmdb_history` on POSIX
  (`Platform.isWindows`-guarded, silent on failure), mirroring
  `DirectorySecretStore` (`DirectoryCredentialStore` at the time this landed).
- **L-1** ✅ **Done** ([PR #69](https://github.com/bettongia/kmdb/pull/69)) —
  `kmdb help <unknown>` caught the `UsageException` (was uncaught → exit 255);
  now prints to stderr and returns 64 (`EX_USAGE`).
- **S-5** ✅ **Complete — [PR #70](https://github.com/bettongia/kmdb/pull/70)**
  (merged 2026-08-09, kmdb-qa PASS). Swapped the hand-rolled vault SHA-256 for
  `cryptography`'s pure-Dart `DartSha256().hashSync()` (not `package:crypto`, not
  the async platform factory — determinism across platforms); kept + hardened the
  hand-rolled **CRC32C** (a vault-format checksum, not a `package:crypto`/
  `cryptography` primitive — analogous to the XXH64 SSTable checksums). QA
  independently ground-truthed every content-address pin → **no address moves**;
  web verified under **dart2wasm** (dart2js is a project non-target per
  `xxhash.dart`). [plan](../plans/completed/plan_0_10_01_s5_vault_hash.md).
- **S-8** ✅ **Done** ([PR #71](https://github.com/bettongia/kmdb/pull/71),
  merged 2026-08-10, kmdb-qa PASS; [plan](../plans/completed/plan_0_10_01_s8_extractor_bounds.md)).
  `ExtractorLimits` (32 MiB / depth 512 / 20 s) constructor-injected from core
  `kmdb` into all four extractors; PDF time bound = cumulative wall-clock budget
  across the `async*` page stream + the existing 30 s `kWorkTimeout` backstop +
  manager-side V1 re-spawn-on-timeout & V2 stale-result guard. Native-hang stays
  RC-25. Not format-freeze sensitive.
- **F-1** ➡️ **Defers out of this repo.** External repos: the `betto_zstd` half
  rides **WI-8** (same repo/PR, must precede WI-9); the `betto_icu` half is a
  separate external change with no release gate.

### WI-7: QA advisories from the PR #61 review (2026-07-20)

Advisory items deliberately **not** actioned in PR #61, to keep that change
scoped:

- **A3 — quarantine is invisible to the embedding application.** ✅ **Done**
  ([PR #72](https://github.com/bettongia/kmdb/pull/72), kmdb-qa PASS, merged
  2026-08-10; [plan](../plans/completed/plan_0_10_01_a3_quarantine_reporting.md)).
  `pull()` used to
  return `Future<void>` and report a skipped peer file only via `print`; because
  `consolidate()` also skips it, a *legitimately* corrupt file was **permanently
  skipped** with no programmatic signal. Fixed: `pull()` → `PullResult` /
  `sync()` → `SyncResult` (`quarantined` vs. transient `deferred`, distinct
  types), plus a durable device-local `$$quarantine` log (never synced) queryable
  via `KmdbDatabase.quarantinedSstables()` / cleared via `clearQuarantineLog()`.
  The log write is committed before the HWM advances (fail-safe crash ordering).
  Breaking `Future<void>`→result signature; lands before the 0.1.0 freeze.
- **A5 — breaking public API changes with no CHANGELOG note.**
  `ConsolidationCoordinator` is exported from `kmdb.dart` and gained a *required*
  `dbDir`; vault blobs gained an unconditional 1-byte envelope prefix. Both are
  fine pre-publication under the no-migration decision, but the CHANGELOG still
  reads only "Initial pub.dev release." Fold into Group D before tagging.
- **A6 — confirm the Windows staging path under RC-3.** `_stagingDir` builds
  `'$dbDir/tmp/consolidation'` with a literal `/` in a fix whose stated purpose
  was Windows compatibility. It matches the existing `VaultStore.stagingDir`
  precedent and Dart's `File` accepts forward slashes on Windows, so this is
  consistency rather than drift — but it wants confirming on a real Windows host.
- **A2 — `close()` can throw after a successful flush.** ✅ **Already done in
  WI-0** (PR #61, commit `15409d0`): `KmdbDatabase.close` now wraps
  `_vaultSearchManager?.close()` in a `try`/`catch` (after the reordering that
  delivered the durability guarantee), so a shutdown failure can no longer escape
  `close()` and be mistaken for a durability failure. No further work.

### WI-5: SC-1 unlock policy

**Blocked on decisions in the proposal's §9** — agent DEK lifetime, WebAuthn PRF
browser support, the default re-authentication interval, and whether
per-operation escalation is in scope.

[Proposal](../proposals/unlock_policy.md). SC-1 is that **a wrong passphrase
opens an encrypted database whenever the DEK cache is warm** — the recommended
mobile configuration. The proposal replaces the raw-DEK cache with 1Password's
wrapped-copy model, which makes the bug class structurally impossible rather
than patching it, and adds a CLI session agent and a headless-server path.

> **§31's own stated remedy does not fix SC-1.** Confirming the cached DEK
> against `enc:blob` proves the *DEK* is valid; the passphrase never enters the
> cache-hit path. **This must not be resolved by editing §31 to match the code.**

### WI-8: `betto_zstd` frame-size cap — closes S-2 ✅ Complete

External repo, own PR and publish (shipped as `betto_zstd 0.1.0-dev.4`,
2026-08-24), plus the KMDB-side consumption plan
([plan_0_10_01_s2_kmdb_decompress_bound.md](../plans/completed/plan_0_10_01_s2_kmdb_decompress_bound.md))
that pins to it and threads `kMaxDecodedValueBytes` through
`ValueCodec.decode`. See the S-2 note above — **both halves have landed and
S-2 is fully closed.**

`betto_zstd` was one of the twelve packages the release gate publishes, so
this had to land before WI-9 or its `0.1.0` would have been burned and needed
republishing.

**F-1** (native allocations before the `try` in `compress`) was folded into
the same `betto_zstd` repo/PR.

### WI-10: W5 / W6 remainder

Review §9 lists what was never exercised. Can run in parallel with anything.

- **W5 (CLI)** — the original list of "never swept" areas (`sync`, `vault`,
  `search`, `index`, `schema`, `encryption`, `import`/`export`/`restore`,
  `promote`/`versions`, the REPL, and `--read` scripts) turned out to be
  **largely stale**. A dedicated 2026-08-23 W5 test-**adequacy** audit
  (`kmdb-qa`, provenance recorded in both plans' headers) checked each area's
  existing tests for actual behavioral coverage (not just line coverage) and
  found:
  - **Already adequate — closed, no further action:** `sync` (dedicated
    `sync_command_test.dart`), `schema` (`schema_command_test.dart`),
    `encryption` (`encryption_command_test.dart`), `import`/`export`/`restore`
    (`ImportCommand`/`ExportCommand` argument-validation, file-round-trip, and
    `Export → Import roundtrip` groups in `commands_test.dart`, plus
    `restore_verify_test.dart`'s `RestoreCommand`/`VerifyCommand` groups), and
    the REPL (13 dedicated test files under `test/repl/`, covering history,
    completion, prompt, colorizer, session state, and all dot-commands).
  - **7 small quick-win gaps** — the poisoned-value "Skipping" guard on
    `dump`/`export`/`scan`, the `search` `query failed` catch, `index create`'s
    success path + `index list`/`info` against a *built* index, the `--read`
    abort-on-first-error default + a mutation round-trip, and `versions`
    delete/`promoted_from` rendering: [W5 CLI coverage quick wins
    plan](../plans/completed/plan_w5_cli_coverage_quick_wins.md) ✅ **Complete**.
  - **1 plan-sized gap** — `vault search`/`reindex`/`status` had no
    positive-path coverage (only the negative/empty-collection paths were
    tested): [vault search CLI positive-path coverage
    plan](../plans/completed/plan_w5_vault_search_cli_coverage.md) ✅ **Complete**.
- **W6 (code health)** — analyzer and the dependency gate were done. **Not**
  done: public API surface audit, dead-code sweep, doc-comment audit, CHANGELOG
  accuracy (overlaps A5).

## Follow-ups discovered during WI-5 (2026-08-22)

Surfaced while landing WI-5 ([PR #75](https://github.com/bettongia/kmdb/pull/75));
neither blocks `0.1.0` and both are tracked as their own plans:

- **[Adopt Dart 3.13.1 / Flutter 3.47.1](../plans/completed/plan_dart_3_13_adoption.md)**
  ✅ **Complete** (2026-08-23; closes the WI-9 prerequisite from the gate
  above). Local dev and CI now both run 3.13.1 / 3.47.1. The former blocker —
  a Linux PDF-indexing failure under 3.13.1 — was **not** a `code_assets` API
  break: it was a Pub-workspace native-library resolution gap (3.13 stages the
  bundled lib to the workspace root `.dart_tool/lib/`, but the loader probed
  only the member package dir), fixed upstream in **betto_pdfium 0.1.0-dev.4**
  (published). Landed: betto_pdfium re-pinned to `0.1.0-dev.4`, both CI
  toolchain pins moved (`dart-lang/setup-dart sdk: 3.13.1` ×4,
  `subosito/flutter-action flutter-version: 3.47.1` ×2, kept version-pinned
  rather than floating), the two `vault_searcher.dart`
  `unawaited_return_in_try_block` lint fixes, a full workspace reformat under
  3.13.1, and every package's `environment: sdk` bumped to `^3.13.0`. The
  toolchain is now Dart 3.13.1 / Flutter 3.47.1 — record this as the project's
  Dart 3.13 minimum in release notes going forward.
- **[LocalDirectoryAdapter CAS unlock ordering](../plans/completed/plan_local_directory_adapter_unlock_ordering.md)**
  ✅ **Complete** ([PR #76](https://github.com/bettongia/kmdb/pull/76), merged
  2026-08-23; kmdb-qa sign-off + delta re-QA). The `atomicCas` update-if-match
  path released the advisory lock and closed the handle *before* the write
  completed; fixed with `return await` on POSIX. The Windows CI lane then
  exposed a platform conflict (the locked ETag re-read needs the target handle
  open, but Windows cannot rename over an open handle), resolved by
  platform-branching `_updateWithLock` — Windows releases before the rename, so
  the unlock-after-write ordering is a **POSIX-only** guarantee (doc-comment and
  §12 updated). Guarded by a deterministic gated-seam ordering probe (skipped on
  Windows; covered there by the atomic-mode conformance suite).

## Deferred to proposals

Design work that is real but must not block `0.1.0`:

- **[betto_secret_store](../proposals/betto_secret_store.md)** — a shared
  Bettongia package providing platform keychain **and** directory-based secret
  storage behind one interface. The Group A plan ships a KMDB-local
  implementation; this proposal is the path to extracting it.
- **[Per-device identity (T3)](../proposals/device_identity.md)** — per-device
  asymmetric keys, enrollment, and revocation, closing the malicious-peer gap.

## Exit criteria

This track is complete — and `0.1.0` is taggable — when **all** of the following
hold:

- [x] **WI-0** — sync trust boundary validation ([PR #61](https://github.com/bettongia/kmdb/pull/61))
- [x] **WI-11** — 🔴 SC-10 (secondary-index **+ FTS + Vec** state) + SC-15 fixed; complete `$meta` device-local vs replicated audit done; equivalence tests added for `query()` **and** `search()` *(before the format freeze)*.
      **Merged [PR #62](https://github.com/bettongia/kmdb/pull/62)** (`kmdb-qa`-signed-off 2026-07-23,
      branch `20260723_plan_0_10_01_index_predicate_trust`).
      Phase 3's audit also found a **sixth** SC-10-shaped instance (the
      dirty-open flag), spun out as **WI-14**.
- [x] **WI-12** — SC-5 `$meta` `device_id` copy retired: the whole path removed
      (read and write), not just the write side — `DEVICE_ID` file is now the
      sole store *(before the format freeze)*
- [x] **WI-13** — `gen:{ns}` classification decided and implemented.
      **Merged [PR #66](https://github.com/bettongia/kmdb/pull/66)** (branch
      `20260803_plan_0_10_01_gen_counter_classification`) — moved to
      `MetaStore.kGenStateNamespace` (`$$genstate`); `LsmEngine.ingestAt0`
      given a compensating ingest-side bump + per-namespace `writeEvent`
      (uniform precise scan); `$cache` reclassified to `$$cache`.
      **Follow-up (perf, non-blocking):** the uniform-precise choice makes
      `ingestAt0` read every data block of the ingested SSTable once
      (`reader.scan()`). QA quantified this: ~1.5 ms for a 64 KB flush, ~110 ms
      for a 20 MB L2 file (O(file size), block-decode CPU). Negligible per
      ingest; the only concerning shape is a **bulk resync** of many large
      consolidated SSTables (~1 GB ⇒ ~5 s added scan CPU). If profiling later
      shows this hurts, the escape hatch is the over-broad alternative
      (bump/emit for all `MetaStore.getNamespaces()`, no scan) or scanning only
      when a session cache / active watchers are present. The cheaper
      index-only approach is **not** correctness-safe (a namespace wholly
      inside one block need not appear in any block's first/last key).
- [x] **WI-14** — dirty-open flag moved off synced `$meta` to a `$$` namespace *(before the format freeze)*
      **Merged 2026-07-29** (PR #65, branch
      `20260727_plan_0_10_01_dirty_flag_meta_retire`) — moved to
      `MetaStore.kDirtyStateNamespace` (`$$dirtystate`); mandatory
      cross-device LWW-erasure regression added and verified to fail with the
      namespace change reverted.
- [x] **WI-1** — W1 complete, **including §13** (audited in pass 3, 2026-07-20 —
      produced SC-15/SC-17…SC-20)
- [x] **WI-2** — SC-2, SC-3 (all 5 sites), SC-11, SC-12, SC-13, SC-14 corrected.
      All spec SC corrections landed (batches `cfffc67`, `b469663`,
      `8dff7ec`, `cc843d7`; SC-16 spun out as its own code fix, [PR #67](https://github.com/bettongia/kmdb/pull/67)).
      The last residual — the stale **code** doc-comment
      `vec_index_state.dart:119` ("for `$meta` storage" → `$$vecstate`) — fixed 2026-08-06.
- [x] **WI-3** — E-2 value AAD landed *(breaking format change — before the freeze)* — [PR #68](https://github.com/bettongia/kmdb/pull/68), merged 2026-08-09 (kmdb-qa PASS; format bump 1→2)
- [x] **WI-4** — T1 sync authentication landed. `SecretStore` precursor
      **done** ([PR #73](https://github.com/bettongia/kmdb/pull/73),
      [plan](../plans/completed/plan_0_10_01_secret_store.md)); the crypto core
      (`SyncAuthenticator`, six-class HKDF sub-keys, `SyncAuthEnvelope`, the
      `SyncAuthenticatingAdapter` decorator, Q1 quarantine composition, vault
      manual threading, `remote pair` enrollment, §34 spec) merged
      ([PR #74](https://github.com/bettongia/kmdb/pull/74), `kmdb-qa` PASS,
      merged 2026-08-10).
- [x] **WI-5** — SC-1 unlock policy landed ([PR #75](https://github.com/bettongia/kmdb/pull/75), merged 2026-08-22)
- [x] **WI-6** — smaller independents cleared. **C-1 done**
      ([plan](../plans/completed/plan_0_10_01_secret_store.md) — core
      `SecretStore` + profile-directory `DirectorySecretStore`, closing the
      Windows dbDir-location gap); C-2 + L-1 done
      ([PR #69](https://github.com/bettongia/kmdb/pull/69)); **S-5** done
      ([PR #70](https://github.com/bettongia/kmdb/pull/70)); **S-8** done
      ([PR #71](https://github.com/bettongia/kmdb/pull/71)); **F-1** tracked
      under WI-8 (betto_zstd) + an external betto_icu change
- [x] **WI-7** — QA advisories cleared. A3 landed ([PR #72](https://github.com/bettongia/kmdb/pull/72), merged 2026-08-10); A2 done in WI-0; A5 → WI-10, A6 → RC-3
- [x] **WI-8** — `betto_zstd` frame cap landed (`0.1.0-dev.4`) and KMDB now
      consumes it via `ValueCodec.decode`'s `maxOutputBytes` threading
      ([plan](../plans/completed/plan_0_10_01_s2_kmdb_decompress_bound.md)),
      **S-2 fully closed**
- [x] **WI-10** — W5/W6 remainder completed (W5: PRs #78/#79 + `builtAt` #80;
      W6: [PR #85](https://github.com/bettongia/kmdb/pull/85) + spec-auditor
      `c0b8666`, 2026-08-26)
- [ ] **WI-9** — release dependency gate, **run last** — Phases A/B/C +
      release-ninja review done (#1/#4/#5 landed, #2 SAHPool export landed
      [PR #88](https://github.com/bettongia/kmdb/pull/88), #3 kmdb_icloud macOS
      CI build deferred to [v0.2.0](0_20.md)); §28 RC-items + Phase D tag remain

And the two cross-cutting checks:

- [ ] Every finding in the review's §6 register is resolved, waived with a
      recorded rationale, or carried to a named later release.
- [ ] `docs/spec/28_release_checklist.md` entries applicable to these changes
      have been run — including **RC-3** (Windows, which covers A6) and
      **RC-25**.

### Verification snapshot (2026-07-20, post-WI-0) — ⚠️ **HISTORICAL, now stale**

> **⚠️ These numbers are a 2026-07-20 snapshot taken right after WI-0 merged.
> They predate WI-11, WI-12, WI-13, WI-14 and SC-16 (PRs #62, #63, #65, #66,
> #67), so the test counts and coverage figure below are **out of date** and
> must not be read as current.** Re-run the suite before tagging `0.1.0` and
> refresh this table; the checks to run are still the right ones.

| Check | Result |
| :--- | :--- |
| `kmdb` tests | 2394 pass / 12 skipped |
| `kmdb_cli` tests | 1176 pass / 3 skipped |
| `make analyze` (9 packages) | Clean |
| Coverage | **94.8%** (11,181/11,800) — above the 90% floor, ~0.2pp below the 95% baseline |
| §18 benchmarks | **10/10 PASS** — but `Scan` is at **74% of budget**; re-check after WI-3, which adds per-value AAD to that path |
