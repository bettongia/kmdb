---
title: 0.03 Roadmap
subtitle: Interconnected, planetary
toc-title: "Contents"
...

# v0.03

## Cloud Adapter: Google Drive

Plans:

[ ]
[Harness mixed-storage mode + behavioural cloud-API simulation](../plans/plan_harness_mixed_storage.md)
[ ] [Google Drive sync](../plans/plan_google_drive_sync.md)

Directly connect to Google Drive for storing sync objects such as the SSTables
etc.

- Drive REST API + `If-Match` ETag
- `GcsAdapter` (GCS JSON API), implements `SyncStorageAdapter`
- Each adapter lives under `lib/src/sync/cloud/`.

## SAHPool OPFS ✅

Plan: [plans/completed/plan_sahpool_opfs.md](../plans/completed/plan_sahpool_opfs.md)

Replaced `StorageAdapterWeb` with `StorageAdapterSahPool`, routing all OPFS I/O
through a Web Worker using `FileSystemSyncAccessHandle` for 3–4× throughput.
Per-op handle lifecycle (open → write → flush → close) provides durability.
Real cross-tab exclusion via exclusive session-held lock handle.
